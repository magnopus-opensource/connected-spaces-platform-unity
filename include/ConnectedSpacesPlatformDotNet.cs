//------------------------------------------------------------------------------
// <auto-generated />
//
// This file was automatically generated by SWIG (https://www.swig.org).
// Version 4.4.0
//
// Do not make changes to this file unless you know what you are doing - modify
// the SWIG interface file instead.
//------------------------------------------------------------------------------


public class ConnectedSpacesPlatformDotNet {

/// <summary>
/// Static class to hold references to callbacks, preventing them from being garbage collected while they are still needed.
/// Callbacks should be added to this set when they are created, and removed once they have been invoked.
/// </summary>
/// <remarks>
/// When handling callbacks while the async method is running or the action adapter that makes use of it exists, we need
/// to be careful to keep a reference to the callback safely in memory until it is called. Otherwise, the garbage 
/// collector may collect it before it is invoked, leading to unexpected behavior or SIGSEGV. To do that, we use this 
/// custom class that holds all the callbacks inside an HashSet that prevents leaks due to premature garbage collection.
/// </remarks>
internal static class AsyncLifetime
{
    /// <summary>
    /// ConcurrentDictionary to hold references to callbacks. This ensures that callbacks are not garbage collected while they are
    /// still needed. Callbacks should be added to this set when they are created, and removed once they have been invoked.
    /// </summary>
    private static readonly System.Collections.Concurrent.ConcurrentDictionary<object, byte> _roots = new();
    
    /// <summary>
    /// Roots the callback, preventing it from being garbage collected. This should be called when the callback is created,
    /// to ensure it stays alive until it is invoked.
    /// </summary> 
    /// <param name="callback">The callback to root.</param>
    internal static void Root(object callback)
    {
        if (callback == null)
        {
            return;
        }
        
        // Only add to root if not already rooted
        if(IsRooted(callback))
        {
            // This should never happen
            System.Diagnostics.Debug.Fail($"Attempted to root a callback that was already rooted: {callback}");
            return;
        }
    
        // Atomic, thread-safe add
        if (!_roots.TryAdd(callback, 0))
        {
          // This should never happen
          System.Diagnostics.Debug.Fail($"Attempted to root a callback that was already rooted: {callback}");
        }
    }

    /// <summary>
    /// Returns true if the specified callback was rooted, false otherwise.
    /// </summary>
    /// <param name="callback">The callback being checked.</param>
    /// <returns>True if already rooted, false otherwise.</returns>
    internal static bool IsRooted(object callback)
    {
        if (callback == null)
        {
            return false;
        }
        
        return _roots.ContainsKey(callback);
    }
    
    /// <summary>
    /// Unroots the callback, allowing it to be garbage collected if there are no other references to it. 
    /// This should be called once the callback has been invoked, to prevent memory leaks.
    /// </summary> 
    /// <param name="callback">The callback to unroot.</param>
    internal static void Unroot(object callback)
    {
        if (callback == null)
        {
            return;
        }
    
        // Atomic, thread-safe remove
        if (!_roots.TryRemove(callback, out _))
        {
          // This should never happen
          System.Diagnostics.Debug.Fail($"Attempted to unroot a callback that was not rooted: {callback}");
        }
    }
}


public sealed class LogCallback : LogCallbackHandlerAdapter
{
    public LogCallback(){}

    public LogCallback(System.Action<csp.common.LogLevel,string> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.common.LogLevel,string>? _invoked;

    public event System.Action<csp.common.LogLevel,string> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.common.LogLevel,string>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.common.LogLevel logLevel,string message)
        => _invoked?.Invoke(logLevel,message);
}


public sealed class EventCallback : EventCallbackHandlerAdapter
{
    public EventCallback(){}

    public EventCallback(System.Action<string> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<string>? _invoked;

    public event System.Action<string> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<string>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(string eventMessage)
        => _invoked?.Invoke(eventMessage);
}


public sealed class BeginMarkerCallback : BeginMarkerCallbackHandlerAdapter
{
    public BeginMarkerCallback(){}

    public BeginMarkerCallback(System.Action<string> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<string>? _invoked;

    public event System.Action<string> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<string>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(string beginMarker)
        => _invoked?.Invoke(beginMarker);
}


public sealed class EndMarkerCallback : EndMarkerCallbackHandlerAdapter
{
    public EndMarkerCallback(){}

    public EndMarkerCallback(System.Action<System.IntPtr> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<System.IntPtr>? _invoked;

    public event System.Action<System.IntPtr> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<System.IntPtr>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(System.IntPtr irrelevant)
        => _invoked?.Invoke(irrelevant);
}


public sealed class UpdateCallback : SpaceEntityUpdatedCallbackAdapter
{
    public UpdateCallback(){}

    public UpdateCallback(System.Action<csp.multiplayer.SpaceEntity,csp.multiplayer.SpaceEntityUpdateFlags,csp.common.ComponentUpdateInfoArray> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.multiplayer.SpaceEntity,csp.multiplayer.SpaceEntityUpdateFlags,csp.common.ComponentUpdateInfoArray>? _invoked;

    public event System.Action<csp.multiplayer.SpaceEntity,csp.multiplayer.SpaceEntityUpdateFlags,csp.common.ComponentUpdateInfoArray> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.multiplayer.SpaceEntity,csp.multiplayer.SpaceEntityUpdateFlags,csp.common.ComponentUpdateInfoArray>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.multiplayer.SpaceEntity spaceEntity,csp.multiplayer.SpaceEntityUpdateFlags updateFlags,csp.common.ComponentUpdateInfoArray componentUpdateInfos)
        => _invoked?.Invoke(spaceEntity,updateFlags,componentUpdateInfos);
}


public sealed class DestroyCallback : BoolCallbackAdapter
{
    public DestroyCallback(){}

    public DestroyCallback(System.Action<bool> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<bool>? _invoked;

    public event System.Action<bool> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<bool>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(bool destroyed)
        => _invoked?.Invoke(destroyed);
}


public sealed class PatchSentCallback : BoolCallbackAdapter
{
    public PatchSentCallback(){}

    public PatchSentCallback(System.Action<bool> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<bool>? _invoked;

    public event System.Action<bool> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<bool>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(bool patchSent)
        => _invoked?.Invoke(patchSent);
}


public sealed class DisconnectionCallback : StringCallbackAdapter
{
    public DisconnectionCallback(){}

    public DisconnectionCallback(System.Action<string> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<string>? _invoked;

    public event System.Action<string> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<string>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(string disconnectReason)
        => _invoked?.Invoke(disconnectReason);
}


public sealed class ConnectionCallback : StringCallbackAdapter
{
    public ConnectionCallback(){}

    public ConnectionCallback(System.Action<string> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<string>? _invoked;

    public event System.Action<string> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<string>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(string connectionStatus)
        => _invoked?.Invoke(connectionStatus);
}


public sealed class NetworkInterruptionCallback : StringCallbackAdapter
{
    public NetworkInterruptionCallback(){}

    public NetworkInterruptionCallback(System.Action<string> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<string>? _invoked;

    public event System.Action<string> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<string>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(string interruptReason)
        => _invoked?.Invoke(interruptReason);
}


public sealed class RemoteEntityCreatedCallback : EntityCreatedCallbackAdapter
{
    public RemoteEntityCreatedCallback(){}

    public RemoteEntityCreatedCallback(System.Action<csp.multiplayer.SpaceEntity> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.multiplayer.SpaceEntity>? _invoked;

    public event System.Action<csp.multiplayer.SpaceEntity> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.multiplayer.SpaceEntity>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.multiplayer.SpaceEntity spaceEntity)
        => _invoked?.Invoke(spaceEntity);
}


public sealed class ScriptLeaderReadyCallback : BoolCallbackAdapter
{
    public ScriptLeaderReadyCallback(){}

    public ScriptLeaderReadyCallback(System.Action<bool> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<bool>? _invoked;

    public event System.Action<bool> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<bool>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(bool ready)
        => _invoked?.Invoke(ready);
}


public sealed class ScopeLeaderCallback : StringStringCallbackAdapter
{
    public ScopeLeaderCallback(){}

    public ScopeLeaderCallback(System.Action<string,string> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<string,string>? _invoked;

    public event System.Action<string,string> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<string,string>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(string scopeId,string userId)
        => _invoked?.Invoke(scopeId,userId);
}


public sealed class EntityFetchCompleteCallback : UInt32CallbackAdapter
{
    public EntityFetchCompleteCallback(){}

    public EntityFetchCompleteCallback(System.Action<uint> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<uint>? _invoked;

    public event System.Action<uint> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<uint>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(uint numEntitiesFetched)
        => _invoked?.Invoke(numEntitiesFetched);
}


public sealed class ConversationUpdateCallback : ConversationNetworkEventCallbackAdapter
{
    public ConversationUpdateCallback(){}

    public ConversationUpdateCallback(System.Action<csp.common.ConversationNetworkEventData> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.common.ConversationNetworkEventData>? _invoked;

    public event System.Action<csp.common.ConversationNetworkEventData> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.common.ConversationNetworkEventData>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.common.ConversationNetworkEventData eventData)
        => _invoked?.Invoke(eventData);
}


public sealed class EntityActionHandler : EntityActionCallbackAdapter
{
    public EntityActionHandler(){}

    public EntityActionHandler(System.Action<csp.multiplayer.ComponentBase,string,string> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.multiplayer.ComponentBase,string,string>? _invoked;

    public event System.Action<csp.multiplayer.ComponentBase,string,string> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.multiplayer.ComponentBase,string,string>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.multiplayer.ComponentBase component,string actionName,string actionParams)
        => _invoked?.Invoke(component,actionName,actionParams);
}


public sealed class AssetDetailBlobChangedCallback : AssetDetailBlobChangedCallbackAdapter
{
    public AssetDetailBlobChangedCallback(){}

    public AssetDetailBlobChangedCallback(System.Action<csp.common.AssetDetailBlobChangedNetworkEventData> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.common.AssetDetailBlobChangedNetworkEventData>? _invoked;

    public event System.Action<csp.common.AssetDetailBlobChangedNetworkEventData> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.common.AssetDetailBlobChangedNetworkEventData>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.common.AssetDetailBlobChangedNetworkEventData eventData)
        => _invoked?.Invoke(eventData);
}


public sealed class MaterialChangedCallback : MaterialChangedCallbackAdapter
{
    public MaterialChangedCallback(){}

    public MaterialChangedCallback(System.Action<csp.common.MaterialChangedParams> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.common.MaterialChangedParams>? _invoked;

    public event System.Action<csp.common.MaterialChangedParams> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.common.MaterialChangedParams>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.common.MaterialChangedParams materialParams)
        => _invoked?.Invoke(materialParams);
}


public sealed class AsyncCallCompletedCallback : AsyncCallCompletedCallbackAdapter
{
    public AsyncCallCompletedCallback(){}

    public AsyncCallCompletedCallback(System.Action<csp.common.AsyncCallCompletedEventData> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.common.AsyncCallCompletedEventData>? _invoked;

    public event System.Action<csp.common.AsyncCallCompletedEventData> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.common.AsyncCallCompletedEventData>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.common.AsyncCallCompletedEventData eventData)
        => _invoked?.Invoke(eventData);
}


public sealed class LoginTokenInfoCallback : LoginTokenInfoResultCallbackAdapter
{
    public LoginTokenInfoCallback(){}

    public LoginTokenInfoCallback(System.Action<csp.systems.LoginTokenInfoResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.LoginTokenInfoResult>? _invoked;

    public event System.Action<csp.systems.LoginTokenInfoResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.LoginTokenInfoResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.LoginTokenInfoResult result)
        => _invoked?.Invoke(result);
}


public sealed class UserPermissionsChangedCallback : UserPermissionsChangedCallbackAdapter
{
    public UserPermissionsChangedCallback(){}

    public UserPermissionsChangedCallback(System.Action<csp.common.AccessControlChangedNetworkEventData> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.common.AccessControlChangedNetworkEventData>? _invoked;

    public event System.Action<csp.common.AccessControlChangedNetworkEventData> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.common.AccessControlChangedNetworkEventData>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.common.AccessControlChangedNetworkEventData eventData)
        => _invoked?.Invoke(eventData);
}


public sealed class SequenceChangedCallback : SequenceChangedCallbackAdapter
{
    public SequenceChangedCallback(){}

    public SequenceChangedCallback(System.Action<csp.common.SequenceChangedNetworkEventData> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.common.SequenceChangedNetworkEventData>? _invoked;

    public event System.Action<csp.common.SequenceChangedNetworkEventData> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.common.SequenceChangedNetworkEventData>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.common.SequenceChangedNetworkEventData eventData)
        => _invoked?.Invoke(eventData);
}


public sealed class HotspotSequenceChangedCallback : SequenceChangedCallbackAdapter
{
    public HotspotSequenceChangedCallback(){}

    public HotspotSequenceChangedCallback(System.Action<csp.common.SequenceChangedNetworkEventData> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.common.SequenceChangedNetworkEventData>? _invoked;

    public event System.Action<csp.common.SequenceChangedNetworkEventData> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.common.SequenceChangedNetworkEventData>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.common.SequenceChangedNetworkEventData eventData)
        => _invoked?.Invoke(eventData);
}


public sealed class NetworkEventCallback : NetworkEventCallbackAdapter
{
    public NetworkEventCallback(){}

    public NetworkEventCallback(System.Action<csp.common.NetworkEventData> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.common.NetworkEventData>? _invoked;

    public event System.Action<csp.common.NetworkEventData> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.common.NetworkEventData>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.common.NetworkEventData networkEventData)
        => _invoked?.Invoke(networkEventData);
}


public sealed class EntityCreatedCallback : EntityCreatedCallbackAdapter
{
    public EntityCreatedCallback(){}

    public EntityCreatedCallback(System.Action<csp.multiplayer.SpaceEntity> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.multiplayer.SpaceEntity>? _invoked;

    public event System.Action<csp.multiplayer.SpaceEntity> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.multiplayer.SpaceEntity>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.multiplayer.SpaceEntity spaceEntity)
        => _invoked?.Invoke(spaceEntity);
}


public sealed class ErrorCodeCallback : ErrorCodeCallbackAdapter
{
    public ErrorCodeCallback(){}

    public ErrorCodeCallback(System.Action<csp.multiplayer.ErrorCode> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.multiplayer.ErrorCode>? _invoked;

    public event System.Action<csp.multiplayer.ErrorCode> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.multiplayer.ErrorCode>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.multiplayer.ErrorCode errorCode)
        => _invoked?.Invoke(errorCode);
}


public sealed class StringResultCallback : StringResultCallbackAdapter
{
    public StringResultCallback(){}

    public StringResultCallback(System.Action<csp.systems.StringResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.StringResult>? _invoked;

    public event System.Action<csp.systems.StringResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.StringResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.StringResult result)
        => _invoked?.Invoke(result);
}


public sealed class NullResultCallback : NullResultCallbackAdapter
{
    public NullResultCallback(){}

    public NullResultCallback(System.Action<csp.systems.NullResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.NullResult>? _invoked;

    public event System.Action<csp.systems.NullResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.NullResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.NullResult result)
        => _invoked?.Invoke(result);
}


public sealed class MessageResultCallback : MessageResultCallbackAdapter
{
    public MessageResultCallback(){}

    public MessageResultCallback(System.Action<csp.multiplayer.MessageResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.multiplayer.MessageResult>? _invoked;

    public event System.Action<csp.multiplayer.MessageResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.multiplayer.MessageResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.multiplayer.MessageResult result)
        => _invoked?.Invoke(result);
}


public sealed class MessageCollectionResultCallback : MessageCollectionResultCallbackAdapter
{
    public MessageCollectionResultCallback(){}

    public MessageCollectionResultCallback(System.Action<csp.multiplayer.MessageCollectionResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.multiplayer.MessageCollectionResult>? _invoked;

    public event System.Action<csp.multiplayer.MessageCollectionResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.multiplayer.MessageCollectionResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.multiplayer.MessageCollectionResult result)
        => _invoked?.Invoke(result);
}


public sealed class ConversationResultCallback : ConversationResultCallbackAdapter
{
    public ConversationResultCallback(){}

    public ConversationResultCallback(System.Action<csp.multiplayer.ConversationResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.multiplayer.ConversationResult>? _invoked;

    public event System.Action<csp.multiplayer.ConversationResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.multiplayer.ConversationResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.multiplayer.ConversationResult result)
        => _invoked?.Invoke(result);
}


public sealed class NumberOfRepliesResultCallback : NumberOfRepliesResultCallbackAdapter
{
    public NumberOfRepliesResultCallback(){}

    public NumberOfRepliesResultCallback(System.Action<csp.multiplayer.NumberOfRepliesResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.multiplayer.NumberOfRepliesResult>? _invoked;

    public event System.Action<csp.multiplayer.NumberOfRepliesResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.multiplayer.NumberOfRepliesResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.multiplayer.NumberOfRepliesResult result)
        => _invoked?.Invoke(result);
}


public sealed class AnnotationResultCallback : AnnotationResultCallbackAdapter
{
    public AnnotationResultCallback(){}

    public AnnotationResultCallback(System.Action<csp.multiplayer.AnnotationResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.multiplayer.AnnotationResult>? _invoked;

    public event System.Action<csp.multiplayer.AnnotationResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.multiplayer.AnnotationResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.multiplayer.AnnotationResult result)
        => _invoked?.Invoke(result);
}


public sealed class AnnotationThumbnailCollectionResultCallback : AnnotationThumbnailCollectionResultCallbackAdapter
{
    public AnnotationThumbnailCollectionResultCallback(){}

    public AnnotationThumbnailCollectionResultCallback(System.Action<csp.multiplayer.AnnotationThumbnailCollectionResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.multiplayer.AnnotationThumbnailCollectionResult>? _invoked;

    public event System.Action<csp.multiplayer.AnnotationThumbnailCollectionResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.multiplayer.AnnotationThumbnailCollectionResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.multiplayer.AnnotationThumbnailCollectionResult result)
        => _invoked?.Invoke(result);
}


public sealed class AssetCollectionResultCallback : AssetCollectionResultCallbackAdapter
{
    public AssetCollectionResultCallback(){}

    public AssetCollectionResultCallback(System.Action<csp.systems.AssetCollectionResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.AssetCollectionResult>? _invoked;

    public event System.Action<csp.systems.AssetCollectionResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.AssetCollectionResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.AssetCollectionResult result)
        => _invoked?.Invoke(result);
}


public sealed class AssetCollectionsResultCallback : AssetCollectionsResultCallbackAdapter
{
    public AssetCollectionsResultCallback(){}

    public AssetCollectionsResultCallback(System.Action<csp.systems.AssetCollectionsResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.AssetCollectionsResult>? _invoked;

    public event System.Action<csp.systems.AssetCollectionsResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.AssetCollectionsResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.AssetCollectionsResult result)
        => _invoked?.Invoke(result);
}


public sealed class AssetCollectionCountResultCallback : AssetCollectionCountResultCallbackAdapter
{
    public AssetCollectionCountResultCallback(){}

    public AssetCollectionCountResultCallback(System.Action<csp.systems.AssetCollectionCountResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.AssetCollectionCountResult>? _invoked;

    public event System.Action<csp.systems.AssetCollectionCountResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.AssetCollectionCountResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.AssetCollectionCountResult result)
        => _invoked?.Invoke(result);
}


public sealed class AssetResultCallback : AssetResultCallbackAdapter
{
    public AssetResultCallback(){}

    public AssetResultCallback(System.Action<csp.systems.AssetResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.AssetResult>? _invoked;

    public event System.Action<csp.systems.AssetResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.AssetResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.AssetResult result)
        => _invoked?.Invoke(result);
}


public sealed class AssetsResultCallback : AssetsResultCallbackAdapter
{
    public AssetsResultCallback(){}

    public AssetsResultCallback(System.Action<csp.systems.AssetsResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.AssetsResult>? _invoked;

    public event System.Action<csp.systems.AssetsResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.AssetsResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.AssetsResult result)
        => _invoked?.Invoke(result);
}


public sealed class UriResultCallback : UriResultCallbackAdapter
{
    public UriResultCallback(){}

    public UriResultCallback(System.Action<csp.systems.UriResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.UriResult>? _invoked;

    public event System.Action<csp.systems.UriResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.UriResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.UriResult result)
        => _invoked?.Invoke(result);
}


public sealed class AssetDataResultCallback : AssetDataResultCallbackAdapter
{
    public AssetDataResultCallback(){}

    public AssetDataResultCallback(System.Action<csp.systems.AssetDataResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.AssetDataResult>? _invoked;

    public event System.Action<csp.systems.AssetDataResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.AssetDataResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.AssetDataResult result)
        => _invoked?.Invoke(result);
}


public sealed class UInt64ResultCallback : UInt64ResultCallbackAdapter
{
    public UInt64ResultCallback(){}

    public UInt64ResultCallback(System.Action<csp.systems.UInt64Result> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.UInt64Result>? _invoked;

    public event System.Action<csp.systems.UInt64Result> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.UInt64Result>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.UInt64Result result)
        => _invoked?.Invoke(result);
}


public sealed class LODChainResultCallback : LODChainResultCallbackAdapter
{
    public LODChainResultCallback(){}

    public LODChainResultCallback(System.Action<csp.systems.LODChainResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.LODChainResult>? _invoked;

    public event System.Action<csp.systems.LODChainResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.LODChainResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.LODChainResult result)
        => _invoked?.Invoke(result);
}


public sealed class MaterialResultCallback : MaterialResultCallbackAdapter
{
    public MaterialResultCallback(){}

    public MaterialResultCallback(System.Action<csp.systems.MaterialResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.MaterialResult>? _invoked;

    public event System.Action<csp.systems.MaterialResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.MaterialResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.MaterialResult result)
        => _invoked?.Invoke(result);
}


public sealed class MaterialsResultCallback : MaterialsResultCallbackAdapter
{
    public MaterialsResultCallback(){}

    public MaterialsResultCallback(System.Action<csp.systems.MaterialsResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.MaterialsResult>? _invoked;

    public event System.Action<csp.systems.MaterialsResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.MaterialsResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.MaterialsResult result)
        => _invoked?.Invoke(result);
}


public sealed class SpaceResultCallback : SpaceResultCallbackAdapter
{
    public SpaceResultCallback(){}

    public SpaceResultCallback(System.Action<csp.systems.SpaceResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.SpaceResult>? _invoked;

    public event System.Action<csp.systems.SpaceResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.SpaceResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.SpaceResult result)
        => _invoked?.Invoke(result);
}


public sealed class BasicSpaceResultCallback : BasicSpaceResultCallbackAdapter
{
    public BasicSpaceResultCallback(){}

    public BasicSpaceResultCallback(System.Action<csp.systems.BasicSpaceResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.BasicSpaceResult>? _invoked;

    public event System.Action<csp.systems.BasicSpaceResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.BasicSpaceResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.BasicSpaceResult result)
        => _invoked?.Invoke(result);
}


public sealed class SpacesResultCallback : SpacesResultCallbackAdapter
{
    public SpacesResultCallback(){}

    public SpacesResultCallback(System.Action<csp.systems.SpacesResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.SpacesResult>? _invoked;

    public event System.Action<csp.systems.SpacesResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.SpacesResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.SpacesResult result)
        => _invoked?.Invoke(result);
}


public sealed class BasicSpacesResultCallback : BasicSpacesResultCallbackAdapter
{
    public BasicSpacesResultCallback(){}

    public BasicSpacesResultCallback(System.Action<csp.systems.BasicSpacesResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.BasicSpacesResult>? _invoked;

    public event System.Action<csp.systems.BasicSpacesResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.BasicSpacesResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.BasicSpacesResult result)
        => _invoked?.Invoke(result);
}


public sealed class PendingInvitesResultCallback : PendingInvitesResultCallbackAdapter
{
    public PendingInvitesResultCallback(){}

    public PendingInvitesResultCallback(System.Action<csp.systems.PendingInvitesResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.PendingInvitesResult>? _invoked;

    public event System.Action<csp.systems.PendingInvitesResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.PendingInvitesResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.PendingInvitesResult result)
        => _invoked?.Invoke(result);
}


public sealed class AcceptedInvitesResultCallback : AcceptedInvitesResultCallbackAdapter
{
    public AcceptedInvitesResultCallback(){}

    public AcceptedInvitesResultCallback(System.Action<csp.systems.AcceptedInvitesResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.AcceptedInvitesResult>? _invoked;

    public event System.Action<csp.systems.AcceptedInvitesResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.AcceptedInvitesResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.AcceptedInvitesResult result)
        => _invoked?.Invoke(result);
}


public sealed class SiteResultCallback : SiteResultCallbackAdapter
{
    public SiteResultCallback(){}

    public SiteResultCallback(System.Action<csp.systems.SiteResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.SiteResult>? _invoked;

    public event System.Action<csp.systems.SiteResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.SiteResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.SiteResult result)
        => _invoked?.Invoke(result);
}


public sealed class SitesCollectionResultCallback : SitesCollectionResultCallbackAdapter
{
    public SitesCollectionResultCallback(){}

    public SitesCollectionResultCallback(System.Action<csp.systems.SitesCollectionResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.SitesCollectionResult>? _invoked;

    public event System.Action<csp.systems.SitesCollectionResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.SitesCollectionResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.SitesCollectionResult result)
        => _invoked?.Invoke(result);
}


public sealed class UserRoleCollectionCallback : UserRoleCollectionCallbackAdapter
{
    public UserRoleCollectionCallback(){}

    public UserRoleCollectionCallback(System.Action<csp.systems.UserRoleCollectionResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.UserRoleCollectionResult>? _invoked;

    public event System.Action<csp.systems.UserRoleCollectionResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.UserRoleCollectionResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.UserRoleCollectionResult result)
        => _invoked?.Invoke(result);
}


public sealed class SpacesMetadataResultCallback : SpacesMetadataResultCallbackAdapter
{
    public SpacesMetadataResultCallback(){}

    public SpacesMetadataResultCallback(System.Action<csp.systems.SpacesMetadataResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.SpacesMetadataResult>? _invoked;

    public event System.Action<csp.systems.SpacesMetadataResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.SpacesMetadataResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.SpacesMetadataResult result)
        => _invoked?.Invoke(result);
}


public sealed class SpaceMetadataResultCallback : SpaceMetadataResultCallbackAdapter
{
    public SpaceMetadataResultCallback(){}

    public SpaceMetadataResultCallback(System.Action<csp.systems.SpaceMetadataResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.SpaceMetadataResult>? _invoked;

    public event System.Action<csp.systems.SpaceMetadataResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.SpaceMetadataResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.SpaceMetadataResult result)
        => _invoked?.Invoke(result);
}


public sealed class SpaceGeoLocationResultCallback : SpaceGeoLocationResultCallbackAdapter
{
    public SpaceGeoLocationResultCallback(){}

    public SpaceGeoLocationResultCallback(System.Action<csp.systems.SpaceGeoLocationResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.SpaceGeoLocationResult>? _invoked;

    public event System.Action<csp.systems.SpaceGeoLocationResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.SpaceGeoLocationResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.SpaceGeoLocationResult result)
        => _invoked?.Invoke(result);
}


public sealed class LoginStateResultCallback : LoginStateResultCallbackAdapter
{
    public LoginStateResultCallback(){}

    public LoginStateResultCallback(System.Action<csp.systems.LoginStateResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.LoginStateResult>? _invoked;

    public event System.Action<csp.systems.LoginStateResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.LoginStateResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.LoginStateResult result)
        => _invoked?.Invoke(result);
}


public sealed class ProfileResultCallback : ProfileResultCallbackAdapter
{
    public ProfileResultCallback(){}

    public ProfileResultCallback(System.Action<csp.systems.ProfileResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.ProfileResult>? _invoked;

    public event System.Action<csp.systems.ProfileResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.ProfileResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.ProfileResult result)
        => _invoked?.Invoke(result);
}


public sealed class BasicProfilesResultCallback : BasicProfilesResultCallbackAdapter
{
    public BasicProfilesResultCallback(){}

    public BasicProfilesResultCallback(System.Action<csp.systems.BasicProfilesResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.BasicProfilesResult>? _invoked;

    public event System.Action<csp.systems.BasicProfilesResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.BasicProfilesResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.BasicProfilesResult result)
        => _invoked?.Invoke(result);
}


public sealed class BooleanResultCallback : BooleanResultCallbackAdapter
{
    public BooleanResultCallback(){}

    public BooleanResultCallback(System.Action<csp.systems.BooleanResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.BooleanResult>? _invoked;

    public event System.Action<csp.systems.BooleanResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.BooleanResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.BooleanResult result)
        => _invoked?.Invoke(result);
}


public sealed class StringArrayResultCallback : StringArrayResultCallbackAdapter
{
    public StringArrayResultCallback(){}

    public StringArrayResultCallback(System.Action<csp.systems.StringArrayResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.StringArrayResult>? _invoked;

    public event System.Action<csp.systems.StringArrayResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.StringArrayResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.StringArrayResult result)
        => _invoked?.Invoke(result);
}


public sealed class AvatarInfoResultCallback : AvatarInfoResultCallbackAdapter
{
    public AvatarInfoResultCallback(){}

    public AvatarInfoResultCallback(System.Action<csp.systems.AvatarInfoResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.AvatarInfoResult>? _invoked;

    public event System.Action<csp.systems.AvatarInfoResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.AvatarInfoResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.AvatarInfoResult result)
        => _invoked?.Invoke(result);
}


public sealed class ApplicationSettingsResultCallback : ApplicationSettingsResultCallbackAdapter
{
    public ApplicationSettingsResultCallback(){}

    public ApplicationSettingsResultCallback(System.Action<csp.systems.ApplicationSettingsResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.ApplicationSettingsResult>? _invoked;

    public event System.Action<csp.systems.ApplicationSettingsResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.ApplicationSettingsResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.ApplicationSettingsResult result)
        => _invoked?.Invoke(result);
}


public sealed class FeatureLimitCallback : FeatureLimitCallbackAdapter
{
    public FeatureLimitCallback(){}

    public FeatureLimitCallback(System.Action<csp.systems.FeatureLimitResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.FeatureLimitResult>? _invoked;

    public event System.Action<csp.systems.FeatureLimitResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.FeatureLimitResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.FeatureLimitResult featureLimitResult)
        => _invoked?.Invoke(featureLimitResult);
}


public sealed class FeaturesLimitCallback : FeaturesLimitCallbackAdapter
{
    public FeaturesLimitCallback(){}

    public FeaturesLimitCallback(System.Action<csp.systems.FeaturesLimitResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.FeaturesLimitResult>? _invoked;

    public event System.Action<csp.systems.FeaturesLimitResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.FeaturesLimitResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.FeaturesLimitResult result)
        => _invoked?.Invoke(result);
}


public sealed class UserTierCallback : UserTierCallbackAdapter
{
    public UserTierCallback(){}

    public UserTierCallback(System.Action<csp.systems.UserTierResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.UserTierResult>? _invoked;

    public event System.Action<csp.systems.UserTierResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.UserTierResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.UserTierResult result)
        => _invoked?.Invoke(result);
}


public sealed class FeatureQuotaCallback : FeatureQuotaCallbackAdapter
{
    public FeatureQuotaCallback(){}

    public FeatureQuotaCallback(System.Action<csp.systems.FeatureQuotaResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.FeatureQuotaResult>? _invoked;

    public event System.Action<csp.systems.FeatureQuotaResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.FeatureQuotaResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.FeatureQuotaResult result)
        => _invoked?.Invoke(result);
}


public sealed class FeaturesQuotaCallback : FeaturesQuotaCallbackAdapter
{
    public FeaturesQuotaCallback(){}

    public FeaturesQuotaCallback(System.Action<csp.systems.FeaturesQuotaResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.FeaturesQuotaResult>? _invoked;

    public event System.Action<csp.systems.FeaturesQuotaResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.FeaturesQuotaResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.FeaturesQuotaResult result)
        => _invoked?.Invoke(result);
}


public sealed class ProductInfoResultCallback : ProductInfoResultCallbackAdapter
{
    public ProductInfoResultCallback(){}

    public ProductInfoResultCallback(System.Action<csp.systems.ProductInfoResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.ProductInfoResult>? _invoked;

    public event System.Action<csp.systems.ProductInfoResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.ProductInfoResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.ProductInfoResult result)
        => _invoked?.Invoke(result);
}


public sealed class ProductInfoCollectionResultCallback : ProductInfoCollectionResultCallbackAdapter
{
    public ProductInfoCollectionResultCallback(){}

    public ProductInfoCollectionResultCallback(System.Action<csp.systems.ProductInfoCollectionResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.ProductInfoCollectionResult>? _invoked;

    public event System.Action<csp.systems.ProductInfoCollectionResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.ProductInfoCollectionResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.ProductInfoCollectionResult result)
        => _invoked?.Invoke(result);
}


public sealed class CheckoutInfoResultCallback : CheckoutInfoResultCallbackAdapter
{
    public CheckoutInfoResultCallback(){}

    public CheckoutInfoResultCallback(System.Action<csp.systems.CheckoutInfoResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.CheckoutInfoResult>? _invoked;

    public event System.Action<csp.systems.CheckoutInfoResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.CheckoutInfoResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.CheckoutInfoResult result)
        => _invoked?.Invoke(result);
}


public sealed class CartInfoResultCallback : CartInfoResultCallbackAdapter
{
    public CartInfoResultCallback(){}

    public CartInfoResultCallback(System.Action<csp.systems.CartInfoResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.CartInfoResult>? _invoked;

    public event System.Action<csp.systems.CartInfoResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.CartInfoResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.CartInfoResult result)
        => _invoked?.Invoke(result);
}


public sealed class GetShopifyStoresResultCallback : GetShopifyStoresResultCallbackAdapter
{
    public GetShopifyStoresResultCallback(){}

    public GetShopifyStoresResultCallback(System.Action<csp.systems.GetShopifyStoresResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.GetShopifyStoresResult>? _invoked;

    public event System.Action<csp.systems.GetShopifyStoresResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.GetShopifyStoresResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.GetShopifyStoresResult result)
        => _invoked?.Invoke(result);
}


public sealed class AddShopifyStoreResultCallback : AddShopifyStoreResultCallbackAdapter
{
    public AddShopifyStoreResultCallback(){}

    public AddShopifyStoreResultCallback(System.Action<csp.systems.AddShopifyStoreResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.AddShopifyStoreResult>? _invoked;

    public event System.Action<csp.systems.AddShopifyStoreResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.AddShopifyStoreResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.AddShopifyStoreResult result)
        => _invoked?.Invoke(result);
}


public sealed class ValidateShopifyStoreResultCallback : ValidateShopifyStoreResultCallbackAdapter
{
    public ValidateShopifyStoreResultCallback(){}

    public ValidateShopifyStoreResultCallback(System.Action<csp.systems.ValidateShopifyStoreResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.ValidateShopifyStoreResult>? _invoked;

    public event System.Action<csp.systems.ValidateShopifyStoreResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.ValidateShopifyStoreResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.ValidateShopifyStoreResult result)
        => _invoked?.Invoke(result);
}


public sealed class TicketedEventResultCallback : TicketedEventResultCallbackAdapter
{
    public TicketedEventResultCallback(){}

    public TicketedEventResultCallback(System.Action<csp.systems.TicketedEventResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.TicketedEventResult>? _invoked;

    public event System.Action<csp.systems.TicketedEventResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.TicketedEventResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.TicketedEventResult result)
        => _invoked?.Invoke(result);
}


public sealed class TicketedEventCollectionResultCallback : TicketedEventCollectionResultCallbackAdapter
{
    public TicketedEventCollectionResultCallback(){}

    public TicketedEventCollectionResultCallback(System.Action<csp.systems.TicketedEventCollectionResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.TicketedEventCollectionResult>? _invoked;

    public event System.Action<csp.systems.TicketedEventCollectionResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.TicketedEventCollectionResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.TicketedEventCollectionResult result)
        => _invoked?.Invoke(result);
}


public sealed class EventTicketResultCallback : EventTicketResultCallbackAdapter
{
    public EventTicketResultCallback(){}

    public EventTicketResultCallback(System.Action<csp.systems.EventTicketResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.EventTicketResult>? _invoked;

    public event System.Action<csp.systems.EventTicketResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.EventTicketResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.EventTicketResult result)
        => _invoked?.Invoke(result);
}


public sealed class TicketedEventVendorAuthorizeInfoCallback : TicketedEventVendorAuthInfoResultCallbackAdapter
{
    public TicketedEventVendorAuthorizeInfoCallback(){}

    public TicketedEventVendorAuthorizeInfoCallback(System.Action<csp.systems.TicketedEventVendorAuthInfoResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.TicketedEventVendorAuthInfoResult>? _invoked;

    public event System.Action<csp.systems.TicketedEventVendorAuthInfoResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.TicketedEventVendorAuthInfoResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.TicketedEventVendorAuthInfoResult result)
        => _invoked?.Invoke(result);
}


public sealed class SpaceIsTicketedResultCallback : SpaceIsTicketedResultCallbackAdapter
{
    public SpaceIsTicketedResultCallback(){}

    public SpaceIsTicketedResultCallback(System.Action<csp.systems.SpaceIsTicketedResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.SpaceIsTicketedResult>? _invoked;

    public event System.Action<csp.systems.SpaceIsTicketedResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.SpaceIsTicketedResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.SpaceIsTicketedResult result)
        => _invoked?.Invoke(result);
}


public sealed class MaintenanceInfoCallback : MaintenanceInfoCallbackAdapter
{
    public MaintenanceInfoCallback(){}

    public MaintenanceInfoCallback(System.Action<csp.systems.MaintenanceInfoResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.MaintenanceInfoResult>? _invoked;

    public event System.Action<csp.systems.MaintenanceInfoResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.MaintenanceInfoResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.MaintenanceInfoResult result)
        => _invoked?.Invoke(result);
}


public sealed class GraphQLReceivedCallback : GraphQLReceivedCallbackAdapter
{
    public GraphQLReceivedCallback(){}

    public GraphQLReceivedCallback(System.Action<csp.systems.GraphQLResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.GraphQLResult>? _invoked;

    public event System.Action<csp.systems.GraphQLResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.GraphQLResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.GraphQLResult result)
        => _invoked?.Invoke(result);
}


public sealed class POIResultCallback : POIResultCallbackAdapter
{
    public POIResultCallback(){}

    public POIResultCallback(System.Action<csp.systems.POIResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.POIResult>? _invoked;

    public event System.Action<csp.systems.POIResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.POIResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.POIResult result)
        => _invoked?.Invoke(result);
}


public sealed class POICollectionResultCallback : POICollectionResultCallbackAdapter
{
    public POICollectionResultCallback(){}

    public POICollectionResultCallback(System.Action<csp.systems.POICollectionResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.POICollectionResult>? _invoked;

    public event System.Action<csp.systems.POICollectionResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.POICollectionResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.POICollectionResult result)
        => _invoked?.Invoke(result);
}


public sealed class AnchorResultCallback : AnchorResultCallbackAdapter
{
    public AnchorResultCallback(){}

    public AnchorResultCallback(System.Action<csp.systems.AnchorResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.AnchorResult>? _invoked;

    public event System.Action<csp.systems.AnchorResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.AnchorResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.AnchorResult result)
        => _invoked?.Invoke(result);
}


public sealed class AnchorCollectionResultCallback : AnchorCollectionResultCallbackAdapter
{
    public AnchorCollectionResultCallback(){}

    public AnchorCollectionResultCallback(System.Action<csp.systems.AnchorCollectionResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.AnchorCollectionResult>? _invoked;

    public event System.Action<csp.systems.AnchorCollectionResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.AnchorCollectionResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.AnchorCollectionResult result)
        => _invoked?.Invoke(result);
}


public sealed class AnchorResolutionResultCallback : AnchorResolutionResultCallbackAdapter
{
    public AnchorResolutionResultCallback(){}

    public AnchorResolutionResultCallback(System.Action<csp.systems.AnchorResolutionResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.AnchorResolutionResult>? _invoked;

    public event System.Action<csp.systems.AnchorResolutionResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.AnchorResolutionResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.AnchorResolutionResult result)
        => _invoked?.Invoke(result);
}


public sealed class SequenceResultCallback : SequenceResultCallbackAdapter
{
    public SequenceResultCallback(){}

    public SequenceResultCallback(System.Action<csp.systems.SequenceResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.SequenceResult>? _invoked;

    public event System.Action<csp.systems.SequenceResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.SequenceResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.SequenceResult result)
        => _invoked?.Invoke(result);
}


public sealed class SequencesResultCallback : SequencesResultCallbackAdapter
{
    public SequencesResultCallback(){}

    public SequencesResultCallback(System.Action<csp.systems.SequencesResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.SequencesResult>? _invoked;

    public event System.Action<csp.systems.SequencesResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.SequencesResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.SequencesResult result)
        => _invoked?.Invoke(result);
}


public sealed class HotspotGroupResultCallback : HotspotGroupResultCallbackAdapter
{
    public HotspotGroupResultCallback(){}

    public HotspotGroupResultCallback(System.Action<csp.systems.HotspotGroupResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.HotspotGroupResult>? _invoked;

    public event System.Action<csp.systems.HotspotGroupResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.HotspotGroupResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.HotspotGroupResult result)
        => _invoked?.Invoke(result);
}


public sealed class HotspotGroupsResultCallback : HotspotGroupsResultCallbackAdapter
{
    public HotspotGroupsResultCallback(){}

    public HotspotGroupsResultCallback(System.Action<csp.systems.HotspotGroupsResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<csp.systems.HotspotGroupsResult>? _invoked;

    public event System.Action<csp.systems.HotspotGroupsResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<csp.systems.HotspotGroupsResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(csp.systems.HotspotGroupsResult result)
        => _invoked?.Invoke(result);
}

  public static global::System.IntPtr GetFunctionAddress(string Name) {
    global::System.IntPtr ret = ConnectedSpacesPlatformDotNetPINVOKE.GetFunctionAddress(Name);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public static void Free(global::System.IntPtr Pointer) {
    ConnectedSpacesPlatformDotNetPINVOKE.Free(Pointer);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public static SWIGTYPE_p_std__string ErrorCodeToString(csp.multiplayer.ErrorCode ErrorCode) {
    SWIGTYPE_p_std__string ret = new SWIGTYPE_p_std__string(ConnectedSpacesPlatformDotNetPINVOKE.ErrorCodeToString((int)ErrorCode), true);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }


public sealed class TestBooleanResultCallback : TestBooleanResultCallbackAdapter
{
    public TestBooleanResultCallback(){}

    public TestBooleanResultCallback(System.Action<extra.test.TestBooleanResult> callbackAction) 
    {
        Invoked += callbackAction;
    }

    private System.Action<extra.test.TestBooleanResult>? _invoked;

    public event System.Action<extra.test.TestBooleanResult> Invoked
    {
        add 
        {
            // Make sure we keep the callback in memory, since we now have a subscriber that needs to use it.
            if(!ConnectedSpacesPlatformDotNet.AsyncLifetime.IsRooted(this))
            {
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Root(this);
            }

            if (_invoked == null)
            {
                // First subscriber, create action. Note that this automatically subscribes the passed value.
                _invoked = new System.Action<extra.test.TestBooleanResult>(value);
            }
            else
            {
                // We already had an action existing, just subscribe to it.
                _invoked += value;
            }
        }
        remove
        {
            if (_invoked == null)
            {
                // Nothing to do, we are trying to unsubscribe when the action is already unavailable.
                return;
            }

            _invoked -= value;
            if (!HasSubscribers)
            {
                // No subscriber left, so we remove the callback from the root to avoid memory leaks.
                ConnectedSpacesPlatformDotNet.AsyncLifetime.Unroot(this);
            }
        }
    }

    public bool HasSubscribers => _invoked != null && _invoked.GetInvocationList().Length == 0;

    public override void Call(extra.test.TestBooleanResult result)
        => _invoked?.Invoke(result);
}

}
