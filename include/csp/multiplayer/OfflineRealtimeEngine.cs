//------------------------------------------------------------------------------
// <auto-generated />
//
// This file was automatically generated by SWIG (https://www.swig.org).
// Version 4.4.0
//
// Do not make changes to this file unless you know what you are doing - modify
// the SWIG interface file instead.
//------------------------------------------------------------------------------

namespace csp.multiplayer {

using csp.common;

public class OfflineRealtimeEngine : IRealtimeEngine, global::System.IDisposable {
  private global::System.Runtime.InteropServices.HandleRef swigCPtr;
  protected bool swigCMemOwn;

  internal OfflineRealtimeEngine(global::System.IntPtr cPtr, bool cMemoryOwn) {
    swigCMemOwn = cMemoryOwn;
    swigCPtr = new global::System.Runtime.InteropServices.HandleRef(this, cPtr);
  }

  internal static global::System.Runtime.InteropServices.HandleRef getCPtr(OfflineRealtimeEngine obj) {
    return (obj == null) ? new global::System.Runtime.InteropServices.HandleRef(null, global::System.IntPtr.Zero) : obj.swigCPtr;
  }

  internal static global::System.Runtime.InteropServices.HandleRef swigRelease(OfflineRealtimeEngine obj) {
    if (obj != null) {
      if (!obj.swigCMemOwn)
        throw new global::System.ApplicationException("Cannot release ownership as memory is not owned");
      global::System.Runtime.InteropServices.HandleRef ptr = obj.swigCPtr;
      obj.swigCMemOwn = false;
      obj.Dispose();
      return ptr;
    } else {
      return new global::System.Runtime.InteropServices.HandleRef(null, global::System.IntPtr.Zero);
    }
  }

  ~OfflineRealtimeEngine() {
    Dispose(false);
  }

  public void Dispose() {
    Dispose(true);
    global::System.GC.SuppressFinalize(this);
  }

  protected virtual void Dispose(bool disposing) {
    lock(this) {
      if (swigCPtr.Handle != global::System.IntPtr.Zero) {
        if (swigCMemOwn) {
          swigCMemOwn = false;
          ConnectedSpacesPlatformDotNetPINVOKE.delete_csp_multiplayer_OfflineRealtimeEngine(swigCPtr);
        }
        swigCPtr = new global::System.Runtime.InteropServices.HandleRef(null, global::System.IntPtr.Zero);
      }
    }
  }

  [System.ComponentModel.EditorBrowsable(System.ComponentModel.EditorBrowsableState.Never)]
  global::System.Runtime.InteropServices.HandleRef IRealtimeEngine.GetInterfaceCPtr() {
    return new global::System.Runtime.InteropServices.HandleRef(this, ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OfflineRealtimeEngine_IRealtimeEngine_GetInterfaceCPtr(swigCPtr.Handle));
  }

  public OfflineRealtimeEngine(csp.multiplayer.CSPSceneDescription SceneDescription, csp.common.LogSystem LogSystem, csp.common.IJSScriptRunner RemoteScriptRunner) : this(ConnectedSpacesPlatformDotNetPINVOKE.new_csp_multiplayer_OfflineRealtimeEngine__SWIG_0(csp.multiplayer.CSPSceneDescription.getCPtr(SceneDescription), csp.common.LogSystem.getCPtr(LogSystem), RemoteScriptRunner.GetInterfaceCPtr()), true) {
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public OfflineRealtimeEngine(csp.common.LogSystem LogSystem, csp.common.IJSScriptRunner RemoteScriptRunner) : this(ConnectedSpacesPlatformDotNetPINVOKE.new_csp_multiplayer_OfflineRealtimeEngine__SWIG_1(csp.common.LogSystem.getCPtr(LogSystem), RemoteScriptRunner.GetInterfaceCPtr()), true) {
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public virtual csp.common.RealtimeEngineType GetRealtimeEngineType() {
    csp.common.RealtimeEngineType ret = (csp.common.RealtimeEngineType)ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OfflineRealtimeEngine_GetRealtimeEngineType(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual void CreateAvatar(string Name, string UserId, csp.multiplayer.SpaceTransform Transform, bool IsVisible, csp.multiplayer.AvatarState State, string AvatarId, csp.multiplayer.AvatarPlayMode AvatarPlayMode, csp.multiplayer.LocomotionModel LocomotionModel, EntityCreatedCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OfflineRealtimeEngine_CreateAvatar(swigCPtr, Name, UserId, csp.multiplayer.SpaceTransform.getCPtr(Transform), IsVisible, (int)State, AvatarId, (int)AvatarPlayMode, (int)LocomotionModel, EntityCreatedCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public virtual void CreateEntity(string Name, csp.multiplayer.SpaceTransform Transform, ulong? ParentID, EntityCreatedCallbackAdapter Callback) {
    csp.common.OptUnsignedInt64 opt_ParentID = ParentID.HasValue ? new csp.common.OptUnsignedInt64(ParentID.Value) : new csp.common.OptUnsignedInt64();
    {
      ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OfflineRealtimeEngine_CreateEntity(swigCPtr, Name, csp.multiplayer.SpaceTransform.getCPtr(Transform), csp.common.OptUnsignedInt64.getCPtr(opt_ParentID), EntityCreatedCallbackAdapter.getCPtr(Callback));
      if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    }
  }

  public virtual void DestroyEntity(csp.multiplayer.SpaceEntity Entity, BoolCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OfflineRealtimeEngine_DestroyEntity(swigCPtr, csp.multiplayer.SpaceEntity.getCPtr(Entity), BoolCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public virtual csp.multiplayer.SpaceEntity FindSpaceEntity(string Name) {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OfflineRealtimeEngine_FindSpaceEntity(swigCPtr, Name);
    csp.multiplayer.SpaceEntity ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.multiplayer.SpaceEntity(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual csp.multiplayer.SpaceEntity FindSpaceEntityById(ulong EntityId) {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OfflineRealtimeEngine_FindSpaceEntityById(swigCPtr, EntityId);
    csp.multiplayer.SpaceEntity ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.multiplayer.SpaceEntity(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual csp.multiplayer.SpaceEntity FindSpaceAvatar(string Name) {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OfflineRealtimeEngine_FindSpaceAvatar(swigCPtr, Name);
    csp.multiplayer.SpaceEntity ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.multiplayer.SpaceEntity(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual csp.multiplayer.SpaceEntity FindSpaceObject(string Name) {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OfflineRealtimeEngine_FindSpaceObject(swigCPtr, Name);
    csp.multiplayer.SpaceEntity ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.multiplayer.SpaceEntity(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual csp.multiplayer.SpaceEntity GetEntityByIndex(uint EntityIndex) {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OfflineRealtimeEngine_GetEntityByIndex(swigCPtr, EntityIndex);
    csp.multiplayer.SpaceEntity ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.multiplayer.SpaceEntity(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual csp.multiplayer.SpaceEntity GetAvatarByIndex(uint AvatarIndex) {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OfflineRealtimeEngine_GetAvatarByIndex(swigCPtr, AvatarIndex);
    csp.multiplayer.SpaceEntity ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.multiplayer.SpaceEntity(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual csp.multiplayer.SpaceEntity GetObjectByIndex(uint ObjectIndex) {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OfflineRealtimeEngine_GetObjectByIndex(swigCPtr, ObjectIndex);
    csp.multiplayer.SpaceEntity ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.multiplayer.SpaceEntity(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual uint GetNumEntities() {
    uint ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OfflineRealtimeEngine_GetNumEntities(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual uint GetNumAvatars() {
    uint ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OfflineRealtimeEngine_GetNumAvatars(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual uint GetNumObjects() {
    uint ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OfflineRealtimeEngine_GetNumObjects(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual csp.common.SpaceEntityPtrList GetAllEntities() {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OfflineRealtimeEngine_GetAllEntities(swigCPtr);
    csp.common.SpaceEntityPtrList ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.common.SpaceEntityPtrList(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual csp.common.SpaceEntityPtrList GetRootHierarchyEntities() {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OfflineRealtimeEngine_GetRootHierarchyEntities(swigCPtr);
    csp.common.SpaceEntityPtrList ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.common.SpaceEntityPtrList(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual csp.multiplayer.ModifiableStatus IsEntityModifiable(csp.multiplayer.SpaceEntity SpaceEntity) {
    csp.multiplayer.ModifiableStatus ret = (csp.multiplayer.ModifiableStatus)ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OfflineRealtimeEngine_IsEntityModifiable(swigCPtr, csp.multiplayer.SpaceEntity.getCPtr(SpaceEntity));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public static ulong LocalClientId() {
    ulong ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OfflineRealtimeEngine_LocalClientId();
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }


  public System.Threading.Tasks.Task<csp.multiplayer.SpaceEntity> CreateAvatarAsync(string name,string userId,csp.multiplayer.SpaceTransform transform,bool isVisible,csp.multiplayer.AvatarState state,string avatarId,csp.multiplayer.AvatarPlayMode avatarPlayMode,csp.multiplayer.LocomotionModel locomotionModel, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.SpaceEntity> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.SpaceEntity>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.EntityCreatedCallback callback =
        new ConnectedSpacesPlatformDotNet.EntityCreatedCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.multiplayer.SpaceEntity>? handler = null;
    handler = new ((csp.multiplayer.SpaceEntity spaceEntity) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this spaceEntity param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)spaceEntity is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(CreateAvatarAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(spaceEntity);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(spaceEntity);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    CreateAvatar(name,userId,transform,isVisible,state,avatarId,avatarPlayMode,locomotionModel, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.multiplayer.SpaceEntity> CreateEntityAsync(string name,csp.multiplayer.SpaceTransform transform,ulong? parentId, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.SpaceEntity> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.SpaceEntity>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.EntityCreatedCallback callback =
        new ConnectedSpacesPlatformDotNet.EntityCreatedCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.multiplayer.SpaceEntity>? handler = null;
    handler = new ((csp.multiplayer.SpaceEntity spaceEntity) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this spaceEntity param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)spaceEntity is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(CreateEntityAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(spaceEntity);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(spaceEntity);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    CreateEntity(name,transform,parentId, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<bool> DestroyEntityAsync(csp.multiplayer.SpaceEntity entity, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<bool> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<bool>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.DestroyCallback callback =
        new ConnectedSpacesPlatformDotNet.DestroyCallback();

    // Define the callback that will be called by the C++ code
    System.Action<bool>? handler = null;
    handler = new ((bool success) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this success param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)success is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(DestroyEntityAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(success);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(success);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    DestroyEntity(entity, callback);

    return tcs.Task;
  }


    // Single native action adapter instance for this event
    private ConnectedSpacesPlatformDotNet.EntityFetchCompleteCallback? _OnEntityFetchCompleteAdapter;

    /// <summary>
    /// C# event exposing the native callback.
    /// Subscribers are automatically registered/unregistered with native code.
    /// </summary>
    public event System.Action<uint> OnEntityFetchComplete
    {
        add
        {
            if (_OnEntityFetchCompleteAdapter == null)
            {
                // First subscriber, create adapter. Note that this automatically subscribes the passed value.
                _OnEntityFetchCompleteAdapter = new ConnectedSpacesPlatformDotNet.EntityFetchCompleteCallback(value);
                // Register with native code
                SetEntityFetchCompleteCallback(_OnEntityFetchCompleteAdapter);
            }
            else
            {
                // We already had an adapter existing, just subscribe to the event.
                _OnEntityFetchCompleteAdapter!.Invoked += value;
                // Note: we should not need to call the SetEntityFetchCompleteCallback again since it was supposed to be set on adapter creation.
            }
        }
        remove
        {
            if (_OnEntityFetchCompleteAdapter == null)
            {
                // This should not happen
                var eventNameAdapter = nameof(_OnEntityFetchCompleteAdapter);
                var eventName = nameof(OnEntityFetchComplete);

                System.Console.Error.WriteLine($"{eventNameAdapter} is null when trying to remove subscriber from {eventName}.");

                return;
            }

            _OnEntityFetchCompleteAdapter.Invoked -= value;

            if (!_OnEntityFetchCompleteAdapter.HasSubscribers)
            {
                // Unregister from native code
                SetEntityFetchCompleteCallback(null);

                // No more subscribers, clean up adapter
                _OnEntityFetchCompleteAdapter = null;
            }
        }
    }


  // Any time this object is returned from an outer C++ object via reference, this is set
  // to prevent premature garbage collection causing premature C++ memory deallocation.
  public object OuterObjectPin { private get; set; }

  public void SetEntityFetchCompleteCallback(UInt32CallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OfflineRealtimeEngine_SetEntityFetchCompleteCallback(swigCPtr, UInt32CallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

}

}
