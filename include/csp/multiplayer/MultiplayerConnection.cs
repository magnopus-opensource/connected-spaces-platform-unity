//------------------------------------------------------------------------------
// <auto-generated />
//
// This file was automatically generated by SWIG (https://www.swig.org).
// Version 4.4.0
//
// Do not make changes to this file unless you know what you are doing - modify
// the SWIG interface file instead.
//------------------------------------------------------------------------------

namespace csp.multiplayer {

#nullable enable

public class MultiplayerConnection : global::System.IDisposable {
  private global::System.Runtime.InteropServices.HandleRef swigCPtr;
  protected bool swigCMemOwn;

  internal MultiplayerConnection(global::System.IntPtr cPtr, bool cMemoryOwn) {
    swigCMemOwn = cMemoryOwn;
    swigCPtr = new global::System.Runtime.InteropServices.HandleRef(this, cPtr);
  }

  internal static global::System.Runtime.InteropServices.HandleRef getCPtr(MultiplayerConnection obj) {
    return (obj == null) ? new global::System.Runtime.InteropServices.HandleRef(null, global::System.IntPtr.Zero) : obj.swigCPtr;
  }

  internal static global::System.Runtime.InteropServices.HandleRef swigRelease(MultiplayerConnection obj) {
    if (obj != null) {
      if (!obj.swigCMemOwn)
        throw new global::System.ApplicationException("Cannot release ownership as memory is not owned");
      global::System.Runtime.InteropServices.HandleRef ptr = obj.swigCPtr;
      obj.swigCMemOwn = false;
      obj.Dispose();
      return ptr;
    } else {
      return new global::System.Runtime.InteropServices.HandleRef(null, global::System.IntPtr.Zero);
    }
  }

  ~MultiplayerConnection() {
    Dispose(false);
  }

  public void Dispose() {
    Dispose(true);
    global::System.GC.SuppressFinalize(this);
  }

  protected virtual void Dispose(bool disposing) {
    lock(this) {
      if (swigCPtr.Handle != global::System.IntPtr.Zero) {
        if (swigCMemOwn) {
          swigCMemOwn = false;
          ConnectedSpacesPlatformDotNetPINVOKE.delete_csp_multiplayer_MultiplayerConnection(swigCPtr);
        }
        swigCPtr = new global::System.Runtime.InteropServices.HandleRef(null, global::System.IntPtr.Zero);
      }
    }
  }

  public void SetDisconnectionCallback(StringCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_MultiplayerConnection_SetDisconnectionCallback(swigCPtr, StringCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void SetConnectionCallback(StringCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_MultiplayerConnection_SetConnectionCallback(swigCPtr, StringCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void SetNetworkInterruptionCallback(StringCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_MultiplayerConnection_SetNetworkInterruptionCallback(swigCPtr, StringCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public ulong GetClientId() {
    ulong ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_MultiplayerConnection_GetClientId(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public csp.multiplayer.ConnectionState GetConnectionState() {
    csp.multiplayer.ConnectionState ret = (csp.multiplayer.ConnectionState)ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_MultiplayerConnection_GetConnectionState(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public void SetAllowSelfMessagingFlag(bool AllowSelfMessaging, ErrorCodeCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_MultiplayerConnection_SetAllowSelfMessagingFlag(swigCPtr, AllowSelfMessaging, ErrorCodeCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public bool GetAllowSelfMessagingFlag() {
    bool ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_MultiplayerConnection_GetAllowSelfMessagingFlag(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public csp.multiplayer.OnlineRealtimeEngine GetOnlineRealtimeEngine() {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_MultiplayerConnection_GetOnlineRealtimeEngine(swigCPtr);
    csp.multiplayer.OnlineRealtimeEngine ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.multiplayer.OnlineRealtimeEngine(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public void __CauseFailure() {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_MultiplayerConnection___CauseFailure(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }


  public System.Threading.Tasks.Task<csp.multiplayer.ErrorCode> SetAllowSelfMessagingFlagAsync(bool allowSelfMessaging, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.ErrorCode> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.ErrorCode>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:D:\a\connected-spaces-platform-unity\connected-spaces-platform-unity\interface\swigutils\AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.ErrorCodeCallback callback =
        new ConnectedSpacesPlatformDotNet.ErrorCodeCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.multiplayer.ErrorCode>? handler = null;
    handler = new ((csp.multiplayer.ErrorCode errorCode) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this errorCode param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)errorCode is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(SetAllowSelfMessagingFlagAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(errorCode);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(errorCode);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    SetAllowSelfMessagingFlag(allowSelfMessaging, callback);

    return tcs.Task;
  }


    // Single native action adapter instance for this event
    private ConnectedSpacesPlatformDotNet.ConnectionCallback? _OnConnectionAdapter;

    /// <summary>
    /// C# event exposing the native callback.
    /// Subscribers are automatically registered/unregistered with native code.
    /// </summary>
    public event System.Action<string> OnConnection
    {
        add
        {
            if (_OnConnectionAdapter == null)
            {
                // First subscriber, create adapter. Note that this automatically subscribes the passed value.
                _OnConnectionAdapter = new ConnectedSpacesPlatformDotNet.ConnectionCallback(value);
                // Register with native code
                SetConnectionCallback(_OnConnectionAdapter);
            }
            else
            {
                // We already had an adapter existing, just subscribe to the event.
                _OnConnectionAdapter!.Invoked += value;
                // Note: we should not need to call the SetConnectionCallback again since it was supposed to be set on adapter creation.
            }
        }
        remove
        {
            if (_OnConnectionAdapter == null)
            {
                // This should not happen
                var eventNameAdapter = nameof(_OnConnectionAdapter);
                var eventName = nameof(OnConnection);

                System.Console.Error.WriteLine($"{eventNameAdapter} is null when trying to remove subscriber from {eventName}.");

                return;
            }

            _OnConnectionAdapter.Invoked -= value;

            if (!_OnConnectionAdapter.HasSubscribers)
            {
                // Unregister from native code
                SetConnectionCallback(null);

                // No more subscribers, clean up adapter
                _OnConnectionAdapter = null;
            }
        }
    }



    // Single native action adapter instance for this event
    private ConnectedSpacesPlatformDotNet.DisconnectionCallback? _OnDisconnectionAdapter;

    /// <summary>
    /// C# event exposing the native callback.
    /// Subscribers are automatically registered/unregistered with native code.
    /// </summary>
    public event System.Action<string> OnDisconnection
    {
        add
        {
            if (_OnDisconnectionAdapter == null)
            {
                // First subscriber, create adapter. Note that this automatically subscribes the passed value.
                _OnDisconnectionAdapter = new ConnectedSpacesPlatformDotNet.DisconnectionCallback(value);
                // Register with native code
                SetDisconnectionCallback(_OnDisconnectionAdapter);
            }
            else
            {
                // We already had an adapter existing, just subscribe to the event.
                _OnDisconnectionAdapter!.Invoked += value;
                // Note: we should not need to call the SetDisconnectionCallback again since it was supposed to be set on adapter creation.
            }
        }
        remove
        {
            if (_OnDisconnectionAdapter == null)
            {
                // This should not happen
                var eventNameAdapter = nameof(_OnDisconnectionAdapter);
                var eventName = nameof(OnDisconnection);

                System.Console.Error.WriteLine($"{eventNameAdapter} is null when trying to remove subscriber from {eventName}.");

                return;
            }

            _OnDisconnectionAdapter.Invoked -= value;

            if (!_OnDisconnectionAdapter.HasSubscribers)
            {
                // Unregister from native code
                SetDisconnectionCallback(null);

                // No more subscribers, clean up adapter
                _OnDisconnectionAdapter = null;
            }
        }
    }



    // Single native action adapter instance for this event
    private ConnectedSpacesPlatformDotNet.NetworkInterruptionCallback? _OnNetworkInterruptionAdapter;

    /// <summary>
    /// C# event exposing the native callback.
    /// Subscribers are automatically registered/unregistered with native code.
    /// </summary>
    public event System.Action<string> OnNetworkInterruption
    {
        add
        {
            if (_OnNetworkInterruptionAdapter == null)
            {
                // First subscriber, create adapter. Note that this automatically subscribes the passed value.
                _OnNetworkInterruptionAdapter = new ConnectedSpacesPlatformDotNet.NetworkInterruptionCallback(value);
                // Register with native code
                SetNetworkInterruptionCallback(_OnNetworkInterruptionAdapter);
            }
            else
            {
                // We already had an adapter existing, just subscribe to the event.
                _OnNetworkInterruptionAdapter!.Invoked += value;
                // Note: we should not need to call the SetNetworkInterruptionCallback again since it was supposed to be set on adapter creation.
            }
        }
        remove
        {
            if (_OnNetworkInterruptionAdapter == null)
            {
                // This should not happen
                var eventNameAdapter = nameof(_OnNetworkInterruptionAdapter);
                var eventName = nameof(OnNetworkInterruption);

                System.Console.Error.WriteLine($"{eventNameAdapter} is null when trying to remove subscriber from {eventName}.");

                return;
            }

            _OnNetworkInterruptionAdapter.Invoked -= value;

            if (!_OnNetworkInterruptionAdapter.HasSubscribers)
            {
                // Unregister from native code
                SetNetworkInterruptionCallback(null);

                // No more subscribers, clean up adapter
                _OnNetworkInterruptionAdapter = null;
            }
        }
    }


  // Any time this object is returned from an outer C++ object via reference, this is set
  // to prevent premature garbage collection causing premature C++ memory deallocation.
  public object OuterObjectPin { private get; set; }

}

}
