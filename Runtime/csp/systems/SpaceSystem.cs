//------------------------------------------------------------------------------
// <auto-generated />
//
// This file was automatically generated by SWIG (https://www.swig.org).
// Version 4.4.0
//
// Do not make changes to this file unless you know what you are doing - modify
// the SWIG interface file instead.
//------------------------------------------------------------------------------

namespace csp.systems {

#nullable enable

public class SpaceSystem : csp.systems.SystemBase {
  private global::System.Runtime.InteropServices.HandleRef swigCPtr;

  internal SpaceSystem(global::System.IntPtr cPtr, bool cMemoryOwn) : base(ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_SWIGUpcast(cPtr), cMemoryOwn) {
    swigCPtr = new global::System.Runtime.InteropServices.HandleRef(this, cPtr);
  }

  internal static global::System.Runtime.InteropServices.HandleRef getCPtr(SpaceSystem obj) {
    return (obj == null) ? new global::System.Runtime.InteropServices.HandleRef(null, global::System.IntPtr.Zero) : obj.swigCPtr;
  }

  internal static global::System.Runtime.InteropServices.HandleRef swigRelease(SpaceSystem obj) {
    if (obj != null) {
      if (!obj.swigCMemOwn)
        throw new global::System.ApplicationException("Cannot release ownership as memory is not owned");
      global::System.Runtime.InteropServices.HandleRef ptr = obj.swigCPtr;
      obj.swigCMemOwn = false;
      obj.Dispose();
      return ptr;
    } else {
      return new global::System.Runtime.InteropServices.HandleRef(null, global::System.IntPtr.Zero);
    }
  }

  protected override void Dispose(bool disposing) {
    lock(this) {
      if (swigCPtr.Handle != global::System.IntPtr.Zero) {
        if (swigCMemOwn) {
          swigCMemOwn = false;
          throw new global::System.MethodAccessException("C++ destructor does not have public access");
        }
        swigCPtr = new global::System.Runtime.InteropServices.HandleRef(null, global::System.IntPtr.Zero);
      }
      base.Dispose(disposing);
    }
  }

  public void EnterSpace(string SpaceId, csp.common.IRealtimeEngine RealtimeEngine, SpaceResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_EnterSpace(swigCPtr, SpaceId, RealtimeEngine == null ? new global::System.Runtime.InteropServices.HandleRef(null, global::System.IntPtr.Zero) : RealtimeEngine.GetInterfaceCPtr(), SpaceResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void ExitSpace(NullResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_ExitSpace(swigCPtr, NullResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public bool IsInSpace() {
    bool ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_IsInSpace(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public csp.systems.Space GetCurrentSpace() {
  global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_GetCurrentSpace(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  csp.systems.Space ret = null;
  if (cPtr != global::System.IntPtr.Zero) {
    ret = new csp.systems.Space(cPtr, false);
    ret.OuterObjectPin = this;
  }
  return ret;
}

  public void CreateSpace(string Name, string Description, csp.systems.SpaceAttributes Attributes, csp.systems.InviteUserRoleInfoCollection InviteUsers, csp.common.StringDict Metadata, csp.systems.FileAssetDataSource FileThumbnail, csp.common.StringArray Tags, SpaceResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_CreateSpace(swigCPtr, Name, Description, (int)Attributes, csp.systems.InviteUserRoleInfoCollection.getCPtr(InviteUsers), csp.common.StringDict.getCPtr(Metadata), csp.systems.FileAssetDataSource.getCPtr(FileThumbnail), csp.common.StringArray.getCPtr(Tags), SpaceResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void CreateSpaceWithBuffer(string Name, string Description, csp.systems.SpaceAttributes Attributes, csp.systems.InviteUserRoleInfoCollection InviteUsers, csp.common.StringDict Metadata, csp.systems.BufferAssetDataSource Thumbnail, csp.common.StringArray Tags, SpaceResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_CreateSpaceWithBuffer(swigCPtr, Name, Description, (int)Attributes, csp.systems.InviteUserRoleInfoCollection.getCPtr(InviteUsers), csp.common.StringDict.getCPtr(Metadata), csp.systems.BufferAssetDataSource.getCPtr(Thumbnail), csp.common.StringArray.getCPtr(Tags), SpaceResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void UpdateSpace(string SpaceId, string Name, string Description, csp.systems.SpaceAttributes? Type, csp.common.StringArray Tags, BasicSpaceResultCallbackAdapter Callback) {
    csp.common.OptSpaceAttributes opt_Type = Type.HasValue ? new csp.common.OptSpaceAttributes(Type.Value) : new csp.common.OptSpaceAttributes();
    {
      ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_UpdateSpace(swigCPtr, SpaceId, Name, Description, csp.common.OptSpaceAttributes.getCPtr(opt_Type), csp.common.StringArray.getCPtr(Tags), BasicSpaceResultCallbackAdapter.getCPtr(Callback));
      if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    }
  }

  public void DeleteSpace(string SpaceId, NullResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_DeleteSpace(swigCPtr, SpaceId, NullResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void GetSpaces(SpacesResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_GetSpaces(swigCPtr, SpacesResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void GetSpacesByAttributes(bool? IsDiscoverable, bool? IsArchived, bool? RequiresInvite, int? ResultsSkip, int? ResultsMax, csp.common.StringArray MustContainTags, csp.common.StringArray MustExcludeTags, bool? MustIncludeAllTags, BasicSpacesResultCallbackAdapter Callback) {
    csp.common.OptBool opt_IsDiscoverable = IsDiscoverable.HasValue ? new csp.common.OptBool(IsDiscoverable.Value) : new csp.common.OptBool();
    csp.common.OptBool opt_IsArchived = IsArchived.HasValue ? new csp.common.OptBool(IsArchived.Value) : new csp.common.OptBool();
    csp.common.OptBool opt_RequiresInvite = RequiresInvite.HasValue ? new csp.common.OptBool(RequiresInvite.Value) : new csp.common.OptBool();
    csp.common.OptSignedInt32 opt_ResultsSkip = ResultsSkip.HasValue ? new csp.common.OptSignedInt32(ResultsSkip.Value) : new csp.common.OptSignedInt32();
    csp.common.OptSignedInt32 opt_ResultsMax = ResultsMax.HasValue ? new csp.common.OptSignedInt32(ResultsMax.Value) : new csp.common.OptSignedInt32();
    csp.common.OptBool opt_MustIncludeAllTags = MustIncludeAllTags.HasValue ? new csp.common.OptBool(MustIncludeAllTags.Value) : new csp.common.OptBool();
    {
      ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_GetSpacesByAttributes(swigCPtr, csp.common.OptBool.getCPtr(opt_IsDiscoverable), csp.common.OptBool.getCPtr(opt_IsArchived), csp.common.OptBool.getCPtr(opt_RequiresInvite), csp.common.OptSignedInt32.getCPtr(opt_ResultsSkip), csp.common.OptSignedInt32.getCPtr(opt_ResultsMax), csp.common.StringArray.getCPtr(MustContainTags), csp.common.StringArray.getCPtr(MustExcludeTags), csp.common.OptBool.getCPtr(opt_MustIncludeAllTags), BasicSpacesResultCallbackAdapter.getCPtr(Callback));
      if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    }
  }

  public void GetSpacesByIds(csp.common.StringArray RequestedSpaceIDs, SpacesResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_GetSpacesByIds(swigCPtr, csp.common.StringArray.getCPtr(RequestedSpaceIDs), SpacesResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void GetSpacesForUserId(string UserId, SpacesResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_GetSpacesForUserId(swigCPtr, UserId, SpacesResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void GetSpace(string SpaceId, SpaceResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_GetSpace(swigCPtr, SpaceId, SpaceResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void InviteToSpace(string SpaceId, string Email, bool? IsModeratorRole, string EmailLinkUrl, string SignupUrl, NullResultCallbackAdapter Callback) {
    csp.common.OptBool opt_IsModeratorRole = IsModeratorRole.HasValue ? new csp.common.OptBool(IsModeratorRole.Value) : new csp.common.OptBool();
    {
      ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_InviteToSpace(swigCPtr, SpaceId, Email, csp.common.OptBool.getCPtr(opt_IsModeratorRole), EmailLinkUrl, SignupUrl, NullResultCallbackAdapter.getCPtr(Callback));
      if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    }
  }

  public void BulkInviteToSpace(string SpaceId, csp.systems.InviteUserRoleInfoCollection InviteUsers, NullResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_BulkInviteToSpace(swigCPtr, SpaceId, csp.systems.InviteUserRoleInfoCollection.getCPtr(InviteUsers), NullResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void GetPendingUserInvites(string SpaceId, PendingInvitesResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_GetPendingUserInvites(swigCPtr, SpaceId, PendingInvitesResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void GetAcceptedUserInvites(string SpaceId, AcceptedInvitesResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_GetAcceptedUserInvites(swigCPtr, SpaceId, AcceptedInvitesResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void RemoveUserFromSpace(string SpaceId, string UserId, NullResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_RemoveUserFromSpace(swigCPtr, SpaceId, UserId, NullResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void AddUserToSpace(string SpaceId, string UserId, SpaceResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_AddUserToSpace(swigCPtr, SpaceId, UserId, SpaceResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void AddSiteInfo(string SpaceId, csp.systems.Site SiteInfo, SiteResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_AddSiteInfo(swigCPtr, SpaceId, csp.systems.Site.getCPtr(SiteInfo), SiteResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void RemoveSiteInfo(string SpaceId, csp.systems.Site SiteInfo, NullResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_RemoveSiteInfo(swigCPtr, SpaceId, csp.systems.Site.getCPtr(SiteInfo), NullResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void GetSitesInfo(string SpaceId, SitesCollectionResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_GetSitesInfo(swigCPtr, SpaceId, SitesCollectionResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void UpdateUserRole(string SpaceId, csp.systems.UserRoleInfo NewUserRoleInfo, NullResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_UpdateUserRole(swigCPtr, SpaceId, csp.systems.UserRoleInfo.getCPtr(NewUserRoleInfo), NullResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void GetUsersRoles(string SpaceId, csp.common.StringArray RequestedUserIds, UserRoleCollectionCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_GetUsersRoles(swigCPtr, SpaceId, csp.common.StringArray.getCPtr(RequestedUserIds), UserRoleCollectionCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void UpdateSpaceMetadata(string SpaceId, csp.common.StringDict NewMetadata, NullResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_UpdateSpaceMetadata(swigCPtr, SpaceId, csp.common.StringDict.getCPtr(NewMetadata), NullResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void GetSpacesMetadata(csp.common.StringArray Spaces, SpacesMetadataResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_GetSpacesMetadata(swigCPtr, csp.common.StringArray.getCPtr(Spaces), SpacesMetadataResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void GetSpaceMetadata(string SpaceId, SpaceMetadataResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_GetSpaceMetadata(swigCPtr, SpaceId, SpaceMetadataResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void UpdateSpaceThumbnail(string SpaceId, csp.systems.FileAssetDataSource NewThumbnail, NullResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_UpdateSpaceThumbnail(swigCPtr, SpaceId, csp.systems.FileAssetDataSource.getCPtr(NewThumbnail), NullResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void UpdateSpaceThumbnailWithBuffer(string SpaceId, csp.systems.BufferAssetDataSource NewThumbnail, NullResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_UpdateSpaceThumbnailWithBuffer(swigCPtr, SpaceId, csp.systems.BufferAssetDataSource.getCPtr(NewThumbnail), NullResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void GetSpaceThumbnail(string SpaceId, UriResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_GetSpaceThumbnail(swigCPtr, SpaceId, UriResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void AddUserToSpaceBanList(string SpaceId, string RequestedUserId, NullResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_AddUserToSpaceBanList(swigCPtr, SpaceId, RequestedUserId, NullResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void DeleteUserFromSpaceBanList(string SpaceId, string RequestedUserId, NullResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_DeleteUserFromSpaceBanList(swigCPtr, SpaceId, RequestedUserId, NullResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void UpdateSpaceGeoLocation(string SpaceId, csp.systems.GeoLocation Location, float? Orientation, csp.common.GeoLocationArray GeoFence, SpaceGeoLocationResultCallbackAdapter Callback) {
    csp.common.OptFloat opt_Orientation = Orientation.HasValue ? new csp.common.OptFloat(Orientation.Value) : new csp.common.OptFloat();
    {
      ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_UpdateSpaceGeoLocation(swigCPtr, SpaceId, csp.systems.GeoLocation.getCPtr(Location), csp.common.OptFloat.getCPtr(opt_Orientation), csp.common.GeoLocationArray.getCPtr(GeoFence), SpaceGeoLocationResultCallbackAdapter.getCPtr(Callback));
      if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    }
  }

  public void GetSpaceGeoLocation(string SpaceId, SpaceGeoLocationResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_GetSpaceGeoLocation(swigCPtr, SpaceId, SpaceGeoLocationResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void DeleteSpaceGeoLocation(string SpaceId, NullResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_DeleteSpaceGeoLocation(swigCPtr, SpaceId, NullResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void DuplicateSpace(string SpaceId, string NewName, csp.systems.SpaceAttributes NewAttributes, csp.common.StringArray MemberGroupIds, bool ShallowCopy, SpaceResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_DuplicateSpace(swigCPtr, SpaceId, NewName, (int)NewAttributes, csp.common.StringArray.getCPtr(MemberGroupIds), ShallowCopy, SpaceResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void DuplicateSpaceAsync(string SpaceId, string NewName, csp.systems.SpaceAttributes NewAttributes, csp.common.StringArray MemberGroupIds, bool ShallowCopy, NullResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_DuplicateSpaceAsync(swigCPtr, SpaceId, NewName, (int)NewAttributes, csp.common.StringArray.getCPtr(MemberGroupIds), ShallowCopy, NullResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void SetAsyncCallCompletedCallback(AsyncCallCompletedCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_systems_SpaceSystem_SetAsyncCallCompletedCallback(swigCPtr, AsyncCallCompletedCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }


  public System.Threading.Tasks.Task<csp.systems.SpaceResult> EnterSpaceAsync(string spaceId,csp.common.IRealtimeEngine realtimeEngine, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.SpaceResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.SpaceResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.SpaceResultCallback callback =
        new ConnectedSpacesPlatformDotNet.SpaceResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.SpaceResult>? handler = null;
    handler = new ((csp.systems.SpaceResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(EnterSpaceAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    EnterSpace(spaceId,realtimeEngine, callback);

    return tcs.Task;
  }

  public System.Threading.Tasks.Task<csp.systems.NullResult> ExitSpaceAsync(System.Action<float>? progressCallback = null)
  {  
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.NullResultCallback callback =
        new ConnectedSpacesPlatformDotNet.NullResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.NullResult>? handler = null;
    handler = new ((csp.systems.NullResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(ExitSpaceAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    ExitSpace(callback);

    return tcs.Task;

  }


  public System.Threading.Tasks.Task<csp.systems.SpaceResult> CreateSpaceAsync(string name,string description,csp.systems.SpaceAttributes attributes,csp.systems.InviteUserRoleInfoCollection? inviteUsers,csp.common.StringDict metadata,csp.systems.FileAssetDataSource? fileThumbnail,csp.common.StringArray? tags, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.SpaceResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.SpaceResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.SpaceResultCallback callback =
        new ConnectedSpacesPlatformDotNet.SpaceResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.SpaceResult>? handler = null;
    handler = new ((csp.systems.SpaceResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(CreateSpaceAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    CreateSpace(name,description,attributes,inviteUsers,metadata,fileThumbnail,tags, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.SpaceResult> CreateSpaceWithBufferAsync(string name,string description,csp.systems.SpaceAttributes attributes,csp.systems.InviteUserRoleInfoCollection? inviteUsers,csp.common.StringDict metadata,csp.systems.BufferAssetDataSource thumbnail,csp.common.StringArray? tags, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.SpaceResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.SpaceResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.SpaceResultCallback callback =
        new ConnectedSpacesPlatformDotNet.SpaceResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.SpaceResult>? handler = null;
    handler = new ((csp.systems.SpaceResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(CreateSpaceWithBufferAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    CreateSpaceWithBuffer(name,description,attributes,inviteUsers,metadata,thumbnail,tags, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.BasicSpaceResult> UpdateSpaceAsync(string spaceId,string? name,string? description,csp.systems.SpaceAttributes? type,csp.common.StringArray? tags, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.BasicSpaceResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.BasicSpaceResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.BasicSpaceResultCallback callback =
        new ConnectedSpacesPlatformDotNet.BasicSpaceResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.BasicSpaceResult>? handler = null;
    handler = new ((csp.systems.BasicSpaceResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(UpdateSpaceAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    UpdateSpace(spaceId,name,description,type,tags, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.NullResult> DeleteSpaceAsync(string spaceId, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.NullResultCallback callback =
        new ConnectedSpacesPlatformDotNet.NullResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.NullResult>? handler = null;
    handler = new ((csp.systems.NullResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(DeleteSpaceAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    DeleteSpace(spaceId, callback);

    return tcs.Task;
  }

  public System.Threading.Tasks.Task<csp.systems.SpacesResult> GetSpacesAsync(System.Action<float>? progressCallback = null)
  {  
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.SpacesResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.SpacesResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.SpacesResultCallback callback =
        new ConnectedSpacesPlatformDotNet.SpacesResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.SpacesResult>? handler = null;
    handler = new ((csp.systems.SpacesResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(GetSpacesAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    GetSpaces(callback);

    return tcs.Task;

  }


  public System.Threading.Tasks.Task<csp.systems.BasicSpacesResult> GetSpacesByAttributesAsync(bool? isDiscoverable,bool? isArchived,bool? requiresInvite,int? resultsSkip,int? resultsMax,csp.common.StringArray? mustContainTags,csp.common.StringArray? mustExcludeTags,bool? mustIncludeAllTags, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.BasicSpacesResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.BasicSpacesResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.BasicSpacesResultCallback callback =
        new ConnectedSpacesPlatformDotNet.BasicSpacesResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.BasicSpacesResult>? handler = null;
    handler = new ((csp.systems.BasicSpacesResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(GetSpacesByAttributesAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    GetSpacesByAttributes(isDiscoverable,isArchived,requiresInvite,resultsSkip,resultsMax,mustContainTags,mustExcludeTags,mustIncludeAllTags, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.SpacesResult> GetSpacesByIdsAsync(csp.common.StringArray requestedSpaceIDs, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.SpacesResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.SpacesResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.SpacesResultCallback callback =
        new ConnectedSpacesPlatformDotNet.SpacesResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.SpacesResult>? handler = null;
    handler = new ((csp.systems.SpacesResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(GetSpacesByIdsAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    GetSpacesByIds(requestedSpaceIDs, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.SpacesResult> GetSpacesForUserIdAsync(string userId, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.SpacesResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.SpacesResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.SpacesResultCallback callback =
        new ConnectedSpacesPlatformDotNet.SpacesResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.SpacesResult>? handler = null;
    handler = new ((csp.systems.SpacesResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(GetSpacesForUserIdAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    GetSpacesForUserId(userId, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.SpaceResult> GetSpaceAsync(string spaceId, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.SpaceResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.SpaceResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.SpaceResultCallback callback =
        new ConnectedSpacesPlatformDotNet.SpaceResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.SpaceResult>? handler = null;
    handler = new ((csp.systems.SpaceResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(GetSpaceAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    GetSpace(spaceId, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.NullResult> InviteToSpaceAsync(string spaceId,string email,bool? isModeratorRole,string? emailLinkUrl,string? signupUrl, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.NullResultCallback callback =
        new ConnectedSpacesPlatformDotNet.NullResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.NullResult>? handler = null;
    handler = new ((csp.systems.NullResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(InviteToSpaceAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    InviteToSpace(spaceId,email,isModeratorRole,emailLinkUrl,signupUrl, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.NullResult> BulkInviteToSpaceAsync(string spaceId,csp.systems.InviteUserRoleInfoCollection inviteUsers, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.NullResultCallback callback =
        new ConnectedSpacesPlatformDotNet.NullResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.NullResult>? handler = null;
    handler = new ((csp.systems.NullResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(BulkInviteToSpaceAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    BulkInviteToSpace(spaceId,inviteUsers, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.PendingInvitesResult> GetPendingUserInvitesAsync(string spaceId, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.PendingInvitesResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.PendingInvitesResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.PendingInvitesResultCallback callback =
        new ConnectedSpacesPlatformDotNet.PendingInvitesResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.PendingInvitesResult>? handler = null;
    handler = new ((csp.systems.PendingInvitesResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(GetPendingUserInvitesAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    GetPendingUserInvites(spaceId, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.AcceptedInvitesResult> GetAcceptedUserInvitesAsync(string spaceId, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.AcceptedInvitesResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.AcceptedInvitesResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.AcceptedInvitesResultCallback callback =
        new ConnectedSpacesPlatformDotNet.AcceptedInvitesResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.AcceptedInvitesResult>? handler = null;
    handler = new ((csp.systems.AcceptedInvitesResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(GetAcceptedUserInvitesAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    GetAcceptedUserInvites(spaceId, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.NullResult> RemoveUserFromSpaceAsync(string spaceId,string userId, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.NullResultCallback callback =
        new ConnectedSpacesPlatformDotNet.NullResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.NullResult>? handler = null;
    handler = new ((csp.systems.NullResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(RemoveUserFromSpaceAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    RemoveUserFromSpace(spaceId,userId, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.SpaceResult> AddUserToSpaceAsync(string spaceId,string userId, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.SpaceResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.SpaceResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.SpaceResultCallback callback =
        new ConnectedSpacesPlatformDotNet.SpaceResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.SpaceResult>? handler = null;
    handler = new ((csp.systems.SpaceResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(AddUserToSpaceAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    AddUserToSpace(spaceId,userId, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.SiteResult> AddSiteInfoAsync(string spaceId,csp.systems.Site siteInfo, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.SiteResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.SiteResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.SiteResultCallback callback =
        new ConnectedSpacesPlatformDotNet.SiteResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.SiteResult>? handler = null;
    handler = new ((csp.systems.SiteResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(AddSiteInfoAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    AddSiteInfo(spaceId,siteInfo, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.NullResult> RemoveSiteInfoAsync(string spaceId,csp.systems.Site siteInfo, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.NullResultCallback callback =
        new ConnectedSpacesPlatformDotNet.NullResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.NullResult>? handler = null;
    handler = new ((csp.systems.NullResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(RemoveSiteInfoAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    RemoveSiteInfo(spaceId,siteInfo, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.SitesCollectionResult> GetSitesInfoAsync(string spaceId, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.SitesCollectionResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.SitesCollectionResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.SitesCollectionResultCallback callback =
        new ConnectedSpacesPlatformDotNet.SitesCollectionResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.SitesCollectionResult>? handler = null;
    handler = new ((csp.systems.SitesCollectionResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(GetSitesInfoAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    GetSitesInfo(spaceId, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.NullResult> UpdateUserRoleAsync(string spaceId,csp.systems.UserRoleInfo newUserRoleInfo, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.NullResultCallback callback =
        new ConnectedSpacesPlatformDotNet.NullResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.NullResult>? handler = null;
    handler = new ((csp.systems.NullResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(UpdateUserRoleAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    UpdateUserRole(spaceId,newUserRoleInfo, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.UserRoleCollectionResult> GetUsersRolesAsync(string spaceId,csp.common.StringArray requestedUserIds, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.UserRoleCollectionResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.UserRoleCollectionResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.UserRoleCollectionCallback callback =
        new ConnectedSpacesPlatformDotNet.UserRoleCollectionCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.UserRoleCollectionResult>? handler = null;
    handler = new ((csp.systems.UserRoleCollectionResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(GetUsersRolesAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    GetUsersRoles(spaceId,requestedUserIds, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.NullResult> UpdateSpaceMetadataAsync(string spaceId,csp.common.StringDict newMetadata, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.NullResultCallback callback =
        new ConnectedSpacesPlatformDotNet.NullResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.NullResult>? handler = null;
    handler = new ((csp.systems.NullResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(UpdateSpaceMetadataAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    UpdateSpaceMetadata(spaceId,newMetadata, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.SpacesMetadataResult> GetSpacesMetadataAsync(csp.common.StringArray spaces, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.SpacesMetadataResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.SpacesMetadataResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.SpacesMetadataResultCallback callback =
        new ConnectedSpacesPlatformDotNet.SpacesMetadataResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.SpacesMetadataResult>? handler = null;
    handler = new ((csp.systems.SpacesMetadataResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(GetSpacesMetadataAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    GetSpacesMetadata(spaces, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.SpaceMetadataResult> GetSpaceMetadataAsync(string spaceId, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.SpaceMetadataResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.SpaceMetadataResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.SpaceMetadataResultCallback callback =
        new ConnectedSpacesPlatformDotNet.SpaceMetadataResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.SpaceMetadataResult>? handler = null;
    handler = new ((csp.systems.SpaceMetadataResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(GetSpaceMetadataAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    GetSpaceMetadata(spaceId, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.NullResult> UpdateSpaceThumbnailAsync(string spaceId,csp.systems.FileAssetDataSource newThumbnail, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.NullResultCallback callback =
        new ConnectedSpacesPlatformDotNet.NullResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.NullResult>? handler = null;
    handler = new ((csp.systems.NullResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(UpdateSpaceThumbnailAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    UpdateSpaceThumbnail(spaceId,newThumbnail, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.NullResult> UpdateSpaceThumbnailWithBufferAsync(string spaceId,csp.systems.BufferAssetDataSource newThumbnail, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.NullResultCallback callback =
        new ConnectedSpacesPlatformDotNet.NullResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.NullResult>? handler = null;
    handler = new ((csp.systems.NullResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(UpdateSpaceThumbnailWithBufferAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    UpdateSpaceThumbnailWithBuffer(spaceId,newThumbnail, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.UriResult> GetSpaceThumbnailAsync(string spaceId, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.UriResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.UriResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.UriResultCallback callback =
        new ConnectedSpacesPlatformDotNet.UriResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.UriResult>? handler = null;
    handler = new ((csp.systems.UriResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(GetSpaceThumbnailAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    GetSpaceThumbnail(spaceId, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.NullResult> AddUserToSpaceBanListAsync(string spaceId,string requestedUserId, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.NullResultCallback callback =
        new ConnectedSpacesPlatformDotNet.NullResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.NullResult>? handler = null;
    handler = new ((csp.systems.NullResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(AddUserToSpaceBanListAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    AddUserToSpaceBanList(spaceId,requestedUserId, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.NullResult> DeleteUserFromSpaceBanListAsync(string spaceId,string requestedUserId, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.NullResultCallback callback =
        new ConnectedSpacesPlatformDotNet.NullResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.NullResult>? handler = null;
    handler = new ((csp.systems.NullResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(DeleteUserFromSpaceBanListAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    DeleteUserFromSpaceBanList(spaceId,requestedUserId, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.SpaceGeoLocationResult> UpdateSpaceGeoLocationAsync(string spaceId,csp.systems.GeoLocation? location,float? orientation,csp.common.GeoLocationArray? geoFence, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.SpaceGeoLocationResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.SpaceGeoLocationResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.SpaceGeoLocationResultCallback callback =
        new ConnectedSpacesPlatformDotNet.SpaceGeoLocationResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.SpaceGeoLocationResult>? handler = null;
    handler = new ((csp.systems.SpaceGeoLocationResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(UpdateSpaceGeoLocationAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    UpdateSpaceGeoLocation(spaceId,location,orientation,geoFence, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.SpaceGeoLocationResult> GetSpaceGeoLocationAsync(string spaceId, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.SpaceGeoLocationResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.SpaceGeoLocationResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.SpaceGeoLocationResultCallback callback =
        new ConnectedSpacesPlatformDotNet.SpaceGeoLocationResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.SpaceGeoLocationResult>? handler = null;
    handler = new ((csp.systems.SpaceGeoLocationResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(GetSpaceGeoLocationAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    GetSpaceGeoLocation(spaceId, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.NullResult> DeleteSpaceGeoLocationAsync(string spaceId, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.NullResultCallback callback =
        new ConnectedSpacesPlatformDotNet.NullResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.NullResult>? handler = null;
    handler = new ((csp.systems.NullResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(DeleteSpaceGeoLocationAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    DeleteSpaceGeoLocation(spaceId, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.SpaceResult> DuplicateSpaceAsync(string spaceId,string newName,csp.systems.SpaceAttributes newAttributes,csp.common.StringArray? memberGroupIds,bool shallowCopy, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.SpaceResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.SpaceResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.SpaceResultCallback callback =
        new ConnectedSpacesPlatformDotNet.SpaceResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.SpaceResult>? handler = null;
    handler = new ((csp.systems.SpaceResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(DuplicateSpaceAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    DuplicateSpace(spaceId,newName,newAttributes,memberGroupIds,shallowCopy, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.NullResult> DuplicateSpaceAsyncAsync(string spaceId,string newName,csp.systems.SpaceAttributes newAttributes,csp.common.StringArray? memberGroupIds,bool shallowCopy, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.NullResultCallback callback =
        new ConnectedSpacesPlatformDotNet.NullResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.NullResult>? handler = null;
    handler = new ((csp.systems.NullResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(DuplicateSpaceAsyncAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    DuplicateSpaceAsync(spaceId,newName,newAttributes,memberGroupIds,shallowCopy, callback);

    return tcs.Task;
  }


    // Single native action adapter instance for this event
    private ConnectedSpacesPlatformDotNet.AsyncCallCompletedCallback? _OnAsyncCallCompletedAdapter;

    /// <summary>
    /// C# event exposing the native callback.
    /// Subscribers are automatically registered/unregistered with native code.
    /// </summary>
    public event System.Action<csp.common.AsyncCallCompletedEventData> OnAsyncCallCompleted
    {
        add
        {
            if (_OnAsyncCallCompletedAdapter == null)
            {
                // First subscriber, create adapter. Note that this automatically subscribes the passed value.
                _OnAsyncCallCompletedAdapter = new ConnectedSpacesPlatformDotNet.AsyncCallCompletedCallback(value);
                // Register with native code
                SetAsyncCallCompletedCallback(_OnAsyncCallCompletedAdapter);
            }
            else
            {
                // We already had an adapter existing, just subscribe to the event.
                _OnAsyncCallCompletedAdapter!.Invoked += value;
                // Note: we should not need to call the SetAsyncCallCompletedCallback again since it was supposed to be set on adapter creation.
            }
        }
        remove
        {
            if (_OnAsyncCallCompletedAdapter == null)
            {
                // This should not happen
                var eventNameAdapter = nameof(_OnAsyncCallCompletedAdapter);
                var eventName = nameof(OnAsyncCallCompleted);

                UnityEngine.Debug.LogError($"{eventNameAdapter} is null when trying to remove subscriber from {eventName}.");

                return;
            }

            _OnAsyncCallCompletedAdapter.Invoked -= value;

            if (!_OnAsyncCallCompletedAdapter.HasSubscribers)
            {
                // Unregister from native code
                SetAsyncCallCompletedCallback(null);

                // No more subscribers, clean up adapter
                _OnAsyncCallCompletedAdapter = null;
            }
        }
    }


  // Any time this object is returned from an outer C++ object via reference, this is set
  // to prevent premature garbage collection causing premature C++ memory deallocation.
  public object OuterObjectPin { private get; set; }

}

}
