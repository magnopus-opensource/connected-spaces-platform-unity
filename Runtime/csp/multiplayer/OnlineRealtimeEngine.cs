//------------------------------------------------------------------------------
// <auto-generated />
//
// This file was automatically generated by SWIG (https://www.swig.org).
// Version 4.4.0
//
// Do not make changes to this file unless you know what you are doing - modify
// the SWIG interface file instead.
//------------------------------------------------------------------------------

namespace csp.multiplayer {

using csp.common;

public class OnlineRealtimeEngine : IRealtimeEngine, global::System.IDisposable {
  private global::System.Runtime.InteropServices.HandleRef swigCPtr;
  protected bool swigCMemOwn;

  internal OnlineRealtimeEngine(global::System.IntPtr cPtr, bool cMemoryOwn) {
    swigCMemOwn = cMemoryOwn;
    swigCPtr = new global::System.Runtime.InteropServices.HandleRef(this, cPtr);
  }

  internal static global::System.Runtime.InteropServices.HandleRef getCPtr(OnlineRealtimeEngine obj) {
    return (obj == null) ? new global::System.Runtime.InteropServices.HandleRef(null, global::System.IntPtr.Zero) : obj.swigCPtr;
  }

  internal static global::System.Runtime.InteropServices.HandleRef swigRelease(OnlineRealtimeEngine obj) {
    if (obj != null) {
      if (!obj.swigCMemOwn)
        throw new global::System.ApplicationException("Cannot release ownership as memory is not owned");
      global::System.Runtime.InteropServices.HandleRef ptr = obj.swigCPtr;
      obj.swigCMemOwn = false;
      obj.Dispose();
      return ptr;
    } else {
      return new global::System.Runtime.InteropServices.HandleRef(null, global::System.IntPtr.Zero);
    }
  }

  ~OnlineRealtimeEngine() {
    Dispose(false);
  }

  public void Dispose() {
    Dispose(true);
    global::System.GC.SuppressFinalize(this);
  }

  protected virtual void Dispose(bool disposing) {
    lock(this) {
      if (swigCPtr.Handle != global::System.IntPtr.Zero) {
        if (swigCMemOwn) {
          swigCMemOwn = false;
          ConnectedSpacesPlatformDotNetPINVOKE.delete_csp_multiplayer_OnlineRealtimeEngine(swigCPtr);
        }
        swigCPtr = new global::System.Runtime.InteropServices.HandleRef(null, global::System.IntPtr.Zero);
      }
    }
  }

  [System.ComponentModel.EditorBrowsable(System.ComponentModel.EditorBrowsableState.Never)]
  global::System.Runtime.InteropServices.HandleRef IRealtimeEngine.GetInterfaceCPtr() {
    return new global::System.Runtime.InteropServices.HandleRef(this, ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_IRealtimeEngine_GetInterfaceCPtr(swigCPtr.Handle));
  }

  public OnlineRealtimeEngine(csp.multiplayer.MultiplayerConnection InMultiplayerConnection, csp.common.LogSystem LogSystem, csp.multiplayer.NetworkEventBus NetworkEventBus, csp.common.IJSScriptRunner RemoteScriptRunner) : this(ConnectedSpacesPlatformDotNetPINVOKE.new_csp_multiplayer_OnlineRealtimeEngine(csp.multiplayer.MultiplayerConnection.getCPtr(InMultiplayerConnection), csp.common.LogSystem.getCPtr(LogSystem), csp.multiplayer.NetworkEventBus.getCPtr(NetworkEventBus), RemoteScriptRunner.GetInterfaceCPtr()), true) {
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public virtual csp.common.RealtimeEngineType GetRealtimeEngineType() {
    csp.common.RealtimeEngineType ret = (csp.common.RealtimeEngineType)ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_GetRealtimeEngineType(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual void CreateAvatar(string Name, string UserId, csp.multiplayer.SpaceTransform SpaceTransform, bool IsVisible, csp.multiplayer.AvatarState State, string AvatarId, csp.multiplayer.AvatarPlayMode AvatarPlayMode, csp.multiplayer.LocomotionModel LocomotionModel, EntityCreatedCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_CreateAvatar(swigCPtr, Name, UserId, csp.multiplayer.SpaceTransform.getCPtr(SpaceTransform), IsVisible, (int)State, AvatarId, (int)AvatarPlayMode, (int)LocomotionModel, EntityCreatedCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public virtual void CreateEntity(string Name, csp.multiplayer.SpaceTransform SpaceTransform, ulong? ParentID, EntityCreatedCallbackAdapter Callback) {
    csp.common.OptUnsignedInt64 opt_ParentID = ParentID.HasValue ? new csp.common.OptUnsignedInt64(ParentID.Value) : new csp.common.OptUnsignedInt64();
    {
      ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_CreateEntity(swigCPtr, Name, csp.multiplayer.SpaceTransform.getCPtr(SpaceTransform), csp.common.OptUnsignedInt64.getCPtr(opt_ParentID), EntityCreatedCallbackAdapter.getCPtr(Callback));
      if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    }
  }

  public virtual void DestroyEntity(csp.multiplayer.SpaceEntity Entity, BoolCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_DestroyEntity(swigCPtr, csp.multiplayer.SpaceEntity.getCPtr(Entity), BoolCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public virtual csp.multiplayer.SpaceEntity FindSpaceEntity(string Name) {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_FindSpaceEntity(swigCPtr, Name);
    csp.multiplayer.SpaceEntity ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.multiplayer.SpaceEntity(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual csp.multiplayer.SpaceEntity FindSpaceEntityById(ulong EntityId) {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_FindSpaceEntityById(swigCPtr, EntityId);
    csp.multiplayer.SpaceEntity ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.multiplayer.SpaceEntity(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual csp.multiplayer.SpaceEntity FindSpaceAvatar(string Name) {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_FindSpaceAvatar(swigCPtr, Name);
    csp.multiplayer.SpaceEntity ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.multiplayer.SpaceEntity(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual csp.multiplayer.SpaceEntity FindSpaceObject(string Name) {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_FindSpaceObject(swigCPtr, Name);
    csp.multiplayer.SpaceEntity ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.multiplayer.SpaceEntity(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual csp.multiplayer.SpaceEntity GetEntityByIndex(uint EntityIndex) {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_GetEntityByIndex(swigCPtr, EntityIndex);
    csp.multiplayer.SpaceEntity ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.multiplayer.SpaceEntity(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual csp.multiplayer.SpaceEntity GetAvatarByIndex(uint AvatarIndex) {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_GetAvatarByIndex(swigCPtr, AvatarIndex);
    csp.multiplayer.SpaceEntity ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.multiplayer.SpaceEntity(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual csp.multiplayer.SpaceEntity GetObjectByIndex(uint ObjectIndex) {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_GetObjectByIndex(swigCPtr, ObjectIndex);
    csp.multiplayer.SpaceEntity ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.multiplayer.SpaceEntity(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual uint GetNumEntities() {
    uint ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_GetNumEntities(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual uint GetNumAvatars() {
    uint ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_GetNumAvatars(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual uint GetNumObjects() {
    uint ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_GetNumObjects(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual csp.common.SpaceEntityPtrList GetAllEntities() {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_GetAllEntities(swigCPtr);
    csp.common.SpaceEntityPtrList ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.common.SpaceEntityPtrList(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual csp.common.SpaceEntityPtrList GetRootHierarchyEntities() {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_GetRootHierarchyEntities(swigCPtr);
    csp.common.SpaceEntityPtrList ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.common.SpaceEntityPtrList(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public virtual csp.multiplayer.ModifiableStatus IsEntityModifiable(csp.multiplayer.SpaceEntity SpaceEntity) {
    csp.multiplayer.ModifiableStatus ret = (csp.multiplayer.ModifiableStatus)ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_IsEntityModifiable(swigCPtr, csp.multiplayer.SpaceEntity.getCPtr(SpaceEntity));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public void QueueEntityUpdate(csp.multiplayer.SpaceEntity Entity) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_QueueEntityUpdate(swigCPtr, csp.multiplayer.SpaceEntity.getCPtr(Entity));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void ProcessPendingEntityOperations() {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_ProcessPendingEntityOperations(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void SetRemoteEntityCreatedCallback(EntityCreatedCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_SetRemoteEntityCreatedCallback(swigCPtr, EntityCreatedCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void SetScriptLeaderReadyCallback(BoolCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_SetScriptLeaderReadyCallback(swigCPtr, BoolCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void SetOnElectedScopeLeaderCallback(StringStringCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_SetOnElectedScopeLeaderCallback(swigCPtr, StringStringCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void SetOnVacatedAsScopeLeaderCallback(StringStringCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_SetOnVacatedAsScopeLeaderCallback(swigCPtr, StringStringCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void ClaimScriptOwnership(csp.multiplayer.SpaceEntity Entity) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_ClaimScriptOwnership(swigCPtr, csp.multiplayer.SpaceEntity.getCPtr(Entity));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void EnableLeaderElection() {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_EnableLeaderElection(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void DisableLeaderElection() {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_DisableLeaderElection(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public bool IsLeaderElectionEnabled() {
    bool ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_IsLeaderElectionEnabled(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public ulong GetLeaderId() {
    ulong ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_GetLeaderId(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public bool GetEntityPatchRateLimitEnabled() {
    bool ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_GetEntityPatchRateLimitEnabled(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public void SetEntityPatchRateLimitEnabled(bool Enabled) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_SetEntityPatchRateLimitEnabled(swigCPtr, Enabled);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }


  public System.Threading.Tasks.Task<csp.multiplayer.SpaceEntity> CreateAvatarAsync(string name,string userId,csp.multiplayer.SpaceTransform spaceTransform,bool isVisible,csp.multiplayer.AvatarState state,string avatarId,csp.multiplayer.AvatarPlayMode avatarPlayMode,csp.multiplayer.LocomotionModel locomotionModel)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.SpaceEntity> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.SpaceEntity>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:D:\a\connected-spaces-platform-unity\connected-spaces-platform-unity\interface\swigutils\AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.EntityCreatedCallback callback =
        new ConnectedSpacesPlatformDotNet.EntityCreatedCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.multiplayer.SpaceEntity>? handler = null;
    handler = new ((csp.multiplayer.SpaceEntity spaceEntity) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this spaceEntity param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)spaceEntity is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(CreateAvatarAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(spaceEntity);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(spaceEntity);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    CreateAvatar(name,userId,spaceTransform,isVisible,state,avatarId,avatarPlayMode,locomotionModel, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.multiplayer.SpaceEntity> CreateEntityAsync(string name,csp.multiplayer.SpaceTransform spaceTransform,ulong? parentId)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.SpaceEntity> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.SpaceEntity>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:D:\a\connected-spaces-platform-unity\connected-spaces-platform-unity\interface\swigutils\AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.EntityCreatedCallback callback =
        new ConnectedSpacesPlatformDotNet.EntityCreatedCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.multiplayer.SpaceEntity>? handler = null;
    handler = new ((csp.multiplayer.SpaceEntity spaceEntity) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this spaceEntity param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)spaceEntity is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(CreateEntityAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(spaceEntity);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(spaceEntity);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    CreateEntity(name,spaceTransform,parentId, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<bool> DestroyEntityAsync(csp.multiplayer.SpaceEntity entity)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<bool> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<bool>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:D:\a\connected-spaces-platform-unity\connected-spaces-platform-unity\interface\swigutils\AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.DestroyCallback callback =
        new ConnectedSpacesPlatformDotNet.DestroyCallback();

    // Define the callback that will be called by the C++ code
    System.Action<bool>? handler = null;
    handler = new ((bool success) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this success param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)success is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(DestroyEntityAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(success);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(success);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    DestroyEntity(entity, callback);

    return tcs.Task;
  }

  // Any time this object is returned from an outer C++ object via reference, this is set
  // to prevent premature garbage collection causing premature C++ memory deallocation.
  public object OuterObjectPin { private get; set; }

  public void SetEntityFetchCompleteCallback(UInt32CallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_OnlineRealtimeEngine_SetEntityFetchCompleteCallback(swigCPtr, UInt32CallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

}

}
