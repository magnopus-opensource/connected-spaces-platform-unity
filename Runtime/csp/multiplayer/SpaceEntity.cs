//------------------------------------------------------------------------------
// <auto-generated />
//
// This file was automatically generated by SWIG (https://www.swig.org).
// Version 4.4.0
//
// Do not make changes to this file unless you know what you are doing - modify
// the SWIG interface file instead.
//------------------------------------------------------------------------------

namespace csp.multiplayer {

#nullable enable

public class SpaceEntity : global::System.IDisposable, System.IEquatable<SpaceEntity> {
  private global::System.Runtime.InteropServices.HandleRef swigCPtr;
  protected bool swigCMemOwn;

  internal SpaceEntity(global::System.IntPtr cPtr, bool cMemoryOwn) {
    swigCMemOwn = cMemoryOwn;
    swigCPtr = new global::System.Runtime.InteropServices.HandleRef(this, cPtr);
  }

  internal static global::System.Runtime.InteropServices.HandleRef getCPtr(SpaceEntity obj) {
    return (obj == null) ? new global::System.Runtime.InteropServices.HandleRef(null, global::System.IntPtr.Zero) : obj.swigCPtr;
  }

  internal static global::System.Runtime.InteropServices.HandleRef swigRelease(SpaceEntity obj) {
    if (obj != null) {
      if (!obj.swigCMemOwn)
        throw new global::System.ApplicationException("Cannot release ownership as memory is not owned");
      global::System.Runtime.InteropServices.HandleRef ptr = obj.swigCPtr;
      obj.swigCMemOwn = false;
      obj.Dispose();
      return ptr;
    } else {
      return new global::System.Runtime.InteropServices.HandleRef(null, global::System.IntPtr.Zero);
    }
  }

  ~SpaceEntity() {
    Dispose(false);
  }

  public void Dispose() {
    Dispose(true);
    global::System.GC.SuppressFinalize(this);
  }

  protected virtual void Dispose(bool disposing) {
    lock(this) {
      if (swigCPtr.Handle != global::System.IntPtr.Zero) {
        if (swigCMemOwn) {
          swigCMemOwn = false;
          ConnectedSpacesPlatformDotNetPINVOKE.delete_csp_multiplayer_SpaceEntity(swigCPtr);
        }
        swigCPtr = new global::System.Runtime.InteropServices.HandleRef(null, global::System.IntPtr.Zero);
      }
    }
  }


  public bool Equals(SpaceEntity? obj)
  {
    if (ReferenceEquals(this, obj)) return true; // The same proxy object
    if (obj is null) return false;
    return SpaceEntity.getCPtr(this).Handle == SpaceEntity.getCPtr(obj).Handle;
  }

  public override bool Equals(object? obj) {
    return Equals(obj as SpaceEntity);
  }

  public override int GetHashCode() {
    //On 32 bit platforms, the additional bits are filled with 0's, making the below cast harmless.
    var href = SpaceEntity.getCPtr(this);
    long ptr = href.Handle.ToInt64();

    // GetHashCode wants a 32 bit int, XOR the ptr with itself, shifting the upper part down, so we
    // avoid collisions with values that have the same lower bits but different higher bits.
    // In the case of a 32 bit system, we just shift 0's so this is a no-op.
    // In checked contexts, this cast from long to int can trigger overflow exceptions.
    // `unchecked` prevents this, overflow is well defined in C#, so hashes remain stable.
    return unchecked((int)(ptr ^ (ptr >> 32)));
  }

  public static bool operator ==(SpaceEntity left, SpaceEntity right)
  {
    if (left is null) return right is null;
    return left.Equals(right);
  }

  public static bool operator !=(SpaceEntity left, SpaceEntity right)
  {
    return !(left == right);
  }


  public SpaceEntity() : this(ConnectedSpacesPlatformDotNetPINVOKE.new_csp_multiplayer_SpaceEntity__SWIG_0(), true) {
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public SpaceEntity(csp.common.IRealtimeEngine InEntitySystem, csp.common.IJSScriptRunner ScriptRunner, csp.common.LogSystem LogSystem) : this(ConnectedSpacesPlatformDotNetPINVOKE.new_csp_multiplayer_SpaceEntity__SWIG_1(InEntitySystem == null ? new global::System.Runtime.InteropServices.HandleRef(null, global::System.IntPtr.Zero) : InEntitySystem.GetInterfaceCPtr(), ScriptRunner.GetInterfaceCPtr(), (LogSystem != null && csp.common.LogSystem.getCPtr(LogSystem).Handle == global::System.IntPtr.Zero) ? throw new global::System.ObjectDisposedException("LogSystem", "Passed a disposed C# SWIG wrapper (null native handle) to C++. This indicates an object lifecycle bug needing investigation. Note: This does not detect C++-side deletions.") : csp.common.LogSystem.getCPtr(LogSystem)), true) {
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public ulong GetId() {
    ulong ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_GetId(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public ulong GetOwnerId() {
    ulong ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_GetOwnerId(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public string GetName() {
    string ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_GetName(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public bool SetName(string Value) {
    bool ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_SetName(swigCPtr, Value);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public csp.multiplayer.SpaceTransform GetTransform() {
  global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_GetTransform(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  csp.multiplayer.SpaceTransform ret = null;
  if (cPtr != global::System.IntPtr.Zero) {
    ret = new csp.multiplayer.SpaceTransform(cPtr, false);
    ret.OuterObjectPin = this;
  }
  return ret;
}

  public csp.multiplayer.SpaceTransform GetGlobalTransform() {
    csp.multiplayer.SpaceTransform ret = new csp.multiplayer.SpaceTransform(ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_GetGlobalTransform(swigCPtr), true);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public csp.common.Vector3 GetPosition() {
  global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_GetPosition(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  csp.common.Vector3 ret = null;
  if (cPtr != global::System.IntPtr.Zero) {
    ret = new csp.common.Vector3(cPtr, false);
    ret.OuterObjectPin = this;
  }
  return ret;
}

  public csp.common.Vector3 GetGlobalPosition() {
    csp.common.Vector3 ret = new csp.common.Vector3(ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_GetGlobalPosition(swigCPtr), true);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public bool SetPosition(csp.common.Vector3 Value) {
    bool ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_SetPosition(swigCPtr, csp.common.Vector3.getCPtr(Value));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public csp.common.Vector4 GetRotation() {
  global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_GetRotation(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  csp.common.Vector4 ret = null;
  if (cPtr != global::System.IntPtr.Zero) {
    ret = new csp.common.Vector4(cPtr, false);
    ret.OuterObjectPin = this;
  }
  return ret;
}

  public csp.common.Vector4 GetGlobalRotation() {
    csp.common.Vector4 ret = new csp.common.Vector4(ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_GetGlobalRotation(swigCPtr), true);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public bool SetRotation(csp.common.Vector4 Value) {
    bool ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_SetRotation(swigCPtr, csp.common.Vector4.getCPtr(Value));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public csp.common.Vector3 GetScale() {
  global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_GetScale(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  csp.common.Vector3 ret = null;
  if (cPtr != global::System.IntPtr.Zero) {
    ret = new csp.common.Vector3(cPtr, false);
    ret.OuterObjectPin = this;
  }
  return ret;
}

  public csp.common.Vector3 GetGlobalScale() {
    csp.common.Vector3 ret = new csp.common.Vector3(ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_GetGlobalScale(swigCPtr), true);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public bool SetScale(csp.common.Vector3 Value) {
    bool ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_SetScale(swigCPtr, csp.common.Vector3.getCPtr(Value));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public bool GetIsTransient() {
    bool ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_GetIsTransient(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public string GetThirdPartyRef() {
    string ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_GetThirdPartyRef(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public bool SetThirdPartyRef(string InThirdPartyRef) {
    bool ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_SetThirdPartyRef(swigCPtr, InThirdPartyRef);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public csp.multiplayer.SpaceEntityType GetEntityType() {
    csp.multiplayer.SpaceEntityType ret = (csp.multiplayer.SpaceEntityType)ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_GetEntityType(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public void SetParentId(ulong ParentId) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_SetParentId(swigCPtr, ParentId);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void RemoveParentEntity() {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_RemoveParentEntity(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public csp.multiplayer.SpaceEntity GetParentEntity() {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_GetParentEntity(swigCPtr);
    csp.multiplayer.SpaceEntity ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.multiplayer.SpaceEntity(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public void CreateChildEntity(string InName, csp.multiplayer.SpaceTransform InSpaceTransform, EntityCreatedCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_CreateChildEntity(swigCPtr, InName, csp.multiplayer.SpaceTransform.getCPtr(InSpaceTransform), EntityCreatedCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public csp.common.SpaceEntityPtrList GetChildEntities() {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_GetChildEntities(swigCPtr);
    csp.common.SpaceEntityPtrList ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.common.SpaceEntityPtrList(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public void Destroy(BoolCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_Destroy(swigCPtr, BoolCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void SetUpdateCallback(SpaceEntityUpdatedCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_SetUpdateCallback(swigCPtr, SpaceEntityUpdatedCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void SetDestroyCallback(BoolCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_SetDestroyCallback(swigCPtr, BoolCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void SetPatchSentCallback(BoolCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_SetPatchSentCallback(swigCPtr, BoolCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public csp.multiplayer.ComponentBase FindFirstComponentOfType(csp.multiplayer.ComponentType Type) {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_FindFirstComponentOfType(swigCPtr, (int)Type);
    csp.multiplayer.ComponentBase ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.multiplayer.ComponentBase(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public csp.common.UInt16ComponentBasePtrDict GetComponents() {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_GetComponents(swigCPtr);
    csp.common.UInt16ComponentBasePtrDict ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.common.UInt16ComponentBasePtrDict(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public csp.multiplayer.ComponentBase GetComponent(ushort Key) {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_GetComponent(swigCPtr, Key);
    csp.multiplayer.ComponentBase ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.multiplayer.ComponentBase(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public csp.multiplayer.ComponentBase AddComponent(csp.multiplayer.ComponentType Type) {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_AddComponent(swigCPtr, (int)Type);
    csp.multiplayer.ComponentBase ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.multiplayer.ComponentBase(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public bool RemoveComponent(ushort Key) {
    bool ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_RemoveComponent(swigCPtr, Key);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public csp.multiplayer.EntityScript GetScript() {
  global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_GetScript(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  csp.multiplayer.EntityScript ret = null;
  if (cPtr != global::System.IntPtr.Zero) {
    ret = new csp.multiplayer.EntityScript(cPtr, false);
    ret.OuterObjectPin = this;
  }
  return ret;
}

  public bool IsSelected() {
    bool ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_IsSelected(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public ulong GetSelectingClientID() {
    ulong ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_GetSelectingClientID(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public bool Select() {
    bool ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_Select(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public bool Deselect() {
    bool ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_Deselect(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public csp.multiplayer.ModifiableStatus IsModifiable() {
    csp.multiplayer.ModifiableStatus ret = (csp.multiplayer.ModifiableStatus)ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_IsModifiable(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public bool Lock() {
    bool ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_Lock(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public bool Unlock() {
    bool ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_Unlock(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public csp.multiplayer.LockType GetLockType() {
    csp.multiplayer.LockType ret = (csp.multiplayer.LockType)ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_GetLockType(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public bool IsLocked() {
    bool ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_IsLocked(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public void QueueUpdate() {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_SpaceEntity_QueueUpdate(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }


  public System.Threading.Tasks.Task<csp.multiplayer.SpaceEntity> CreateChildEntityAsync(string name,csp.multiplayer.SpaceTransform spaceTransform, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.SpaceEntity> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.SpaceEntity>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.EntityCreatedCallback callback =
        new ConnectedSpacesPlatformDotNet.EntityCreatedCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.multiplayer.SpaceEntity>? handler = null;
    handler = new ((csp.multiplayer.SpaceEntity spaceEntity) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this spaceEntity param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)spaceEntity is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(CreateChildEntityAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(spaceEntity);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(spaceEntity);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    CreateChildEntity(name,spaceTransform, callback);

    return tcs.Task;
  }

  public System.Threading.Tasks.Task<bool> DestroyAsync(System.Action<float>? progressCallback = null)
  {  
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<bool> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<bool>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:/Users/runner/work/connected-spaces-platform-unity/connected-spaces-platform-unity/interface/swigutils/AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.DestroyCallback callback =
        new ConnectedSpacesPlatformDotNet.DestroyCallback();

    // Define the callback that will be called by the C++ code
    System.Action<bool>? handler = null;
    handler = new ((bool success) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this success param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)success is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(DestroyAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(success);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(success);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);

            // Ensure the callback is safely unregistered and unpinned from the global 
            // GCHandle registry if a validation exception interrupts the execution flow
            callback.Invoked -= handler;
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    Destroy(callback);

    return tcs.Task;

  }


    // Single native action adapter instance for this event
    private ConnectedSpacesPlatformDotNet.UpdateCallback? _OnUpdateAdapter;

    /// <summary>
    /// C# event exposing the native callback.
    /// Subscribers are automatically registered/unregistered with native code.
    /// </summary>
    public event System.Action<csp.multiplayer.SpaceEntity,csp.multiplayer.SpaceEntityUpdateFlags,csp.common.ComponentUpdateInfoArray> OnUpdate
    {
        add
        {
            if (_OnUpdateAdapter == null)
            {
                // First subscriber, create adapter. Note that this automatically subscribes the passed value.
                _OnUpdateAdapter = new ConnectedSpacesPlatformDotNet.UpdateCallback(value);
                // Register with native code
                SetUpdateCallback(_OnUpdateAdapter);
            }
            else
            {
                // We already had an adapter existing, just subscribe to the event.
                _OnUpdateAdapter!.Invoked += value;
                // Note: we should not need to call the SetUpdateCallback again since it was supposed to be set on adapter creation.
            }
        }
        remove
        {
            if (_OnUpdateAdapter == null)
            {
                // This should not happen
                var eventNameAdapter = nameof(_OnUpdateAdapter);
                var eventName = nameof(OnUpdate);

                UnityEngine.Debug.LogError($"{eventNameAdapter} is null when trying to remove subscriber from {eventName}.");

                return;
            }

            _OnUpdateAdapter.Invoked -= value;

            if (!_OnUpdateAdapter.HasSubscribers)
            {
                // Unregister from native code
                SetUpdateCallback(null);

                // No more subscribers, clean up adapter
                _OnUpdateAdapter = null;
            }
        }
    }



    // Single native action adapter instance for this event
    private ConnectedSpacesPlatformDotNet.DestroyCallback? _OnDestroyAdapter;

    /// <summary>
    /// C# event exposing the native callback.
    /// Subscribers are automatically registered/unregistered with native code.
    /// </summary>
    public event System.Action<bool> OnDestroy
    {
        add
        {
            if (_OnDestroyAdapter == null)
            {
                // First subscriber, create adapter. Note that this automatically subscribes the passed value.
                _OnDestroyAdapter = new ConnectedSpacesPlatformDotNet.DestroyCallback(value);
                // Register with native code
                SetDestroyCallback(_OnDestroyAdapter);
            }
            else
            {
                // We already had an adapter existing, just subscribe to the event.
                _OnDestroyAdapter!.Invoked += value;
                // Note: we should not need to call the SetDestroyCallback again since it was supposed to be set on adapter creation.
            }
        }
        remove
        {
            if (_OnDestroyAdapter == null)
            {
                // This should not happen
                var eventNameAdapter = nameof(_OnDestroyAdapter);
                var eventName = nameof(OnDestroy);

                UnityEngine.Debug.LogError($"{eventNameAdapter} is null when trying to remove subscriber from {eventName}.");

                return;
            }

            _OnDestroyAdapter.Invoked -= value;

            if (!_OnDestroyAdapter.HasSubscribers)
            {
                // Unregister from native code
                SetDestroyCallback(null);

                // No more subscribers, clean up adapter
                _OnDestroyAdapter = null;
            }
        }
    }


  // Any time this object is returned from an outer C++ object via reference, this is set
  // to prevent premature garbage collection causing premature C++ memory deallocation.
  public object OuterObjectPin { private get; set; }

}

}
