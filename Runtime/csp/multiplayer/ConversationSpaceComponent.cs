//------------------------------------------------------------------------------
// <auto-generated />
//
// This file was automatically generated by SWIG (https://www.swig.org).
// Version 4.4.0
//
// Do not make changes to this file unless you know what you are doing - modify
// the SWIG interface file instead.
//------------------------------------------------------------------------------

namespace csp.multiplayer {

#nullable enable

public class ConversationSpaceComponent : csp.multiplayer.ComponentBase, IPositionComponent, IRotationComponent {
  private global::System.Runtime.InteropServices.HandleRef swigCPtr;

  internal ConversationSpaceComponent(global::System.IntPtr cPtr, bool cMemoryOwn) : base(ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_SWIGUpcast(cPtr), cMemoryOwn) {
    swigCPtr = new global::System.Runtime.InteropServices.HandleRef(this, cPtr);
  }

  internal static global::System.Runtime.InteropServices.HandleRef getCPtr(ConversationSpaceComponent obj) {
    return (obj == null) ? new global::System.Runtime.InteropServices.HandleRef(null, global::System.IntPtr.Zero) : obj.swigCPtr;
  }

  internal static global::System.Runtime.InteropServices.HandleRef swigRelease(ConversationSpaceComponent obj) {
    if (obj != null) {
      if (!obj.swigCMemOwn)
        throw new global::System.ApplicationException("Cannot release ownership as memory is not owned");
      global::System.Runtime.InteropServices.HandleRef ptr = obj.swigCPtr;
      obj.swigCMemOwn = false;
      obj.Dispose();
      return ptr;
    } else {
      return new global::System.Runtime.InteropServices.HandleRef(null, global::System.IntPtr.Zero);
    }
  }

  protected override void Dispose(bool disposing) {
    lock(this) {
      if (swigCPtr.Handle != global::System.IntPtr.Zero) {
        if (swigCMemOwn) {
          swigCMemOwn = false;
          ConnectedSpacesPlatformDotNetPINVOKE.delete_csp_multiplayer_ConversationSpaceComponent(swigCPtr);
        }
        swigCPtr = new global::System.Runtime.InteropServices.HandleRef(null, global::System.IntPtr.Zero);
      }
      base.Dispose(disposing);
    }
  }

  [System.ComponentModel.EditorBrowsable(System.ComponentModel.EditorBrowsableState.Never)]
  global::System.Runtime.InteropServices.HandleRef IPositionComponent.GetInterfaceCPtr() {
    return new global::System.Runtime.InteropServices.HandleRef(this, ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_IPositionComponent_GetInterfaceCPtr(swigCPtr.Handle));
  }

  [System.ComponentModel.EditorBrowsable(System.ComponentModel.EditorBrowsableState.Never)]
  global::System.Runtime.InteropServices.HandleRef IRotationComponent.GetInterfaceCPtr() {
    return new global::System.Runtime.InteropServices.HandleRef(this, ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_IRotationComponent_GetInterfaceCPtr(swigCPtr.Handle));
  }


  // Returns null on failure.
  public static csp.multiplayer.ConversationSpaceComponent? TryFromBaseCast(csp.multiplayer.ComponentBase baseObj)
  {
    return NativeFromBaseCast(baseObj);
  }

  // Throws ArgumentException on failure.
  public static csp.multiplayer.ConversationSpaceComponent FromBaseCast(csp.multiplayer.ComponentBase baseObj)
  {
    csp.multiplayer.ConversationSpaceComponent? derived = TryFromBaseCast(baseObj);
    if (derived == null){
      throw new System.ArgumentException("Failed to cast " + nameof(csp.multiplayer.ComponentBase) + " to " + nameof(csp.multiplayer.ConversationSpaceComponent), "baseObj");
    }
    return derived;
  }


  public ConversationSpaceComponent(csp.common.LogSystem LogSystem, csp.multiplayer.SpaceEntity Parent) : this(ConnectedSpacesPlatformDotNetPINVOKE.new_csp_multiplayer_ConversationSpaceComponent(csp.common.LogSystem.getCPtr(LogSystem), csp.multiplayer.SpaceEntity.getCPtr(Parent)), true) {
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void CreateConversation(string Message, StringResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_CreateConversation(swigCPtr, Message, StringResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void DeleteConversation(NullResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_DeleteConversation(swigCPtr, NullResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void AddMessage(string Message, MessageResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_AddMessage(swigCPtr, Message, MessageResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void DeleteMessage(string MessageId, NullResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_DeleteMessage(swigCPtr, MessageId, NullResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void GetMessagesFromConversation(int? ResultsSkipNumber, int? ResultsMaxNumber, MessageCollectionResultCallbackAdapter Callback) {
    csp.common.OptSignedInt32 opt_ResultsSkipNumber = ResultsSkipNumber.HasValue ? new csp.common.OptSignedInt32(ResultsSkipNumber.Value) : new csp.common.OptSignedInt32();
    csp.common.OptSignedInt32 opt_ResultsMaxNumber = ResultsMaxNumber.HasValue ? new csp.common.OptSignedInt32(ResultsMaxNumber.Value) : new csp.common.OptSignedInt32();
    {
      ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_GetMessagesFromConversation(swigCPtr, csp.common.OptSignedInt32.getCPtr(opt_ResultsSkipNumber), csp.common.OptSignedInt32.getCPtr(opt_ResultsMaxNumber), MessageCollectionResultCallbackAdapter.getCPtr(Callback));
      if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    }
  }

  public void GetConversationInfo(ConversationResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_GetConversationInfo(swigCPtr, ConversationResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void UpdateConversation(csp.multiplayer.MessageUpdateParams NewData, ConversationResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_UpdateConversation(swigCPtr, csp.multiplayer.MessageUpdateParams.getCPtr(NewData), ConversationResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void GetMessageInfo(string MessageId, MessageResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_GetMessageInfo(swigCPtr, MessageId, MessageResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void UpdateMessage(string MessageId, csp.multiplayer.MessageUpdateParams NewData, MessageResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_UpdateMessage(swigCPtr, MessageId, csp.multiplayer.MessageUpdateParams.getCPtr(NewData), MessageResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void GetNumberOfReplies(NumberOfRepliesResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_GetNumberOfReplies(swigCPtr, NumberOfRepliesResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void GetConversationAnnotation(AnnotationResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_GetConversationAnnotation(swigCPtr, AnnotationResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void SetConversationAnnotation(csp.multiplayer.AnnotationUpdateParams AnnotationParams, csp.systems.BufferAssetDataSource Annotation, csp.systems.BufferAssetDataSource AnnotationThumbnail, AnnotationResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_SetConversationAnnotation(swigCPtr, csp.multiplayer.AnnotationUpdateParams.getCPtr(AnnotationParams), csp.systems.BufferAssetDataSource.getCPtr(Annotation), csp.systems.BufferAssetDataSource.getCPtr(AnnotationThumbnail), AnnotationResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void DeleteConversationAnnotation(NullResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_DeleteConversationAnnotation(swigCPtr, NullResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void GetAnnotation(string MessageId, AnnotationResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_GetAnnotation(swigCPtr, MessageId, AnnotationResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void SetAnnotation(string MessageId, csp.multiplayer.AnnotationUpdateParams UpdateParams, csp.systems.BufferAssetDataSource Annotation, csp.systems.BufferAssetDataSource AnnotationThumbnail, AnnotationResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_SetAnnotation(swigCPtr, MessageId, csp.multiplayer.AnnotationUpdateParams.getCPtr(UpdateParams), csp.systems.BufferAssetDataSource.getCPtr(Annotation), csp.systems.BufferAssetDataSource.getCPtr(AnnotationThumbnail), AnnotationResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void DeleteAnnotation(string MessageId, NullResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_DeleteAnnotation(swigCPtr, MessageId, NullResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void GetAnnotationThumbnailsForConversation(AnnotationThumbnailCollectionResultCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_GetAnnotationThumbnailsForConversation(swigCPtr, AnnotationThumbnailCollectionResultCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void SetConversationUpdateCallback(ConversationNetworkEventCallbackAdapter Callback) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_SetConversationUpdateCallback(swigCPtr, ConversationNetworkEventCallbackAdapter.getCPtr(Callback));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public virtual csp.common.Vector3 GetPosition() {
  global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_GetPosition(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  csp.common.Vector3 ret = null;
  if (cPtr != global::System.IntPtr.Zero) {
    ret = new csp.common.Vector3(cPtr, false);
    ret.OuterObjectPin = this;
  }
  return ret;
}

  public virtual void SetPosition(csp.common.Vector3 InValue) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_SetPosition(swigCPtr, csp.common.Vector3.getCPtr(InValue));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public virtual csp.common.Vector4 GetRotation() {
  global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_GetRotation(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  csp.common.Vector4 ret = null;
  if (cPtr != global::System.IntPtr.Zero) {
    ret = new csp.common.Vector4(cPtr, false);
    ret.OuterObjectPin = this;
  }
  return ret;
}

  public virtual void SetRotation(csp.common.Vector4 InValue) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_SetRotation(swigCPtr, csp.common.Vector4.getCPtr(InValue));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public bool GetIsVisible() {
    bool ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_GetIsVisible(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public void SetIsVisible(bool Value) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_SetIsVisible(swigCPtr, Value);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public bool GetIsActive() {
    bool ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_GetIsActive(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public void SetIsActive(bool Value) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_SetIsActive(swigCPtr, Value);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public void SetTitle(string Value) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_SetTitle(swigCPtr, Value);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public string GetTitle() {
    string ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_GetTitle(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public void SetResolved(bool Value) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_SetResolved(swigCPtr, Value);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public bool GetResolved() {
    bool ret = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_GetResolved(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

  public void SetConversationCameraPosition(csp.common.Vector3 InValue) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_SetConversationCameraPosition(swigCPtr, csp.common.Vector3.getCPtr(InValue));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public csp.common.Vector3 GetConversationCameraPosition() {
  global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_GetConversationCameraPosition(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  csp.common.Vector3 ret = null;
  if (cPtr != global::System.IntPtr.Zero) {
    ret = new csp.common.Vector3(cPtr, false);
    ret.OuterObjectPin = this;
  }
  return ret;
}

  public void SetConversationCameraRotation(csp.common.Vector4 InValue) {
    ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_SetConversationCameraRotation(swigCPtr, csp.common.Vector4.getCPtr(InValue));
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  }

  public csp.common.Vector4 GetConversationCameraRotation() {
  global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_GetConversationCameraRotation(swigCPtr);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
  csp.common.Vector4 ret = null;
  if (cPtr != global::System.IntPtr.Zero) {
    ret = new csp.common.Vector4(cPtr, false);
    ret.OuterObjectPin = this;
  }
  return ret;
}


  public System.Threading.Tasks.Task<csp.systems.StringResult> CreateConversationAsync(string message, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.StringResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.StringResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:D:\a\connected-spaces-platform-unity\connected-spaces-platform-unity\interface\swigutils\AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.StringResultCallback callback =
        new ConnectedSpacesPlatformDotNet.StringResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.StringResult>? handler = null;
    handler = new ((csp.systems.StringResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(CreateConversationAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    CreateConversation(message, callback);

    return tcs.Task;
  }

  public System.Threading.Tasks.Task<csp.systems.NullResult> DeleteConversationAsync(System.Action<float>? progressCallback = null)
  {  
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:D:\a\connected-spaces-platform-unity\connected-spaces-platform-unity\interface\swigutils\AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.NullResultCallback callback =
        new ConnectedSpacesPlatformDotNet.NullResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.NullResult>? handler = null;
    handler = new ((csp.systems.NullResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(DeleteConversationAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    DeleteConversation(callback);

    return tcs.Task;

  }


  public System.Threading.Tasks.Task<csp.multiplayer.MessageResult> AddMessageAsync(string message, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.MessageResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.MessageResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:D:\a\connected-spaces-platform-unity\connected-spaces-platform-unity\interface\swigutils\AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.MessageResultCallback callback =
        new ConnectedSpacesPlatformDotNet.MessageResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.multiplayer.MessageResult>? handler = null;
    handler = new ((csp.multiplayer.MessageResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(AddMessageAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    AddMessage(message, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.NullResult> DeleteMessageAsync(string messageId, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:D:\a\connected-spaces-platform-unity\connected-spaces-platform-unity\interface\swigutils\AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.NullResultCallback callback =
        new ConnectedSpacesPlatformDotNet.NullResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.NullResult>? handler = null;
    handler = new ((csp.systems.NullResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(DeleteMessageAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    DeleteMessage(messageId, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.multiplayer.MessageCollectionResult> GetMessagesFromConversationAsync(int? resultsSkipNumber,int? resultsMaxNumber, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.MessageCollectionResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.MessageCollectionResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:D:\a\connected-spaces-platform-unity\connected-spaces-platform-unity\interface\swigutils\AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.MessageCollectionResultCallback callback =
        new ConnectedSpacesPlatformDotNet.MessageCollectionResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.multiplayer.MessageCollectionResult>? handler = null;
    handler = new ((csp.multiplayer.MessageCollectionResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(GetMessagesFromConversationAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    GetMessagesFromConversation(resultsSkipNumber,resultsMaxNumber, callback);

    return tcs.Task;
  }

  public System.Threading.Tasks.Task<csp.multiplayer.ConversationResult> GetConversationInfoAsync(System.Action<float>? progressCallback = null)
  {  
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.ConversationResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.ConversationResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:D:\a\connected-spaces-platform-unity\connected-spaces-platform-unity\interface\swigutils\AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.ConversationResultCallback callback =
        new ConnectedSpacesPlatformDotNet.ConversationResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.multiplayer.ConversationResult>? handler = null;
    handler = new ((csp.multiplayer.ConversationResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(GetConversationInfoAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    GetConversationInfo(callback);

    return tcs.Task;

  }


  public System.Threading.Tasks.Task<csp.multiplayer.ConversationResult> UpdateConversationAsync(csp.multiplayer.MessageUpdateParams newData, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.ConversationResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.ConversationResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:D:\a\connected-spaces-platform-unity\connected-spaces-platform-unity\interface\swigutils\AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.ConversationResultCallback callback =
        new ConnectedSpacesPlatformDotNet.ConversationResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.multiplayer.ConversationResult>? handler = null;
    handler = new ((csp.multiplayer.ConversationResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(UpdateConversationAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    UpdateConversation(newData, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.multiplayer.MessageResult> GetMessageInfoAsync(string messageId, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.MessageResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.MessageResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:D:\a\connected-spaces-platform-unity\connected-spaces-platform-unity\interface\swigutils\AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.MessageResultCallback callback =
        new ConnectedSpacesPlatformDotNet.MessageResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.multiplayer.MessageResult>? handler = null;
    handler = new ((csp.multiplayer.MessageResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(GetMessageInfoAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    GetMessageInfo(messageId, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.multiplayer.MessageResult> UpdateMessageAsync(string messageId,csp.multiplayer.MessageUpdateParams newData, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.MessageResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.MessageResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:D:\a\connected-spaces-platform-unity\connected-spaces-platform-unity\interface\swigutils\AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.MessageResultCallback callback =
        new ConnectedSpacesPlatformDotNet.MessageResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.multiplayer.MessageResult>? handler = null;
    handler = new ((csp.multiplayer.MessageResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(UpdateMessageAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    UpdateMessage(messageId,newData, callback);

    return tcs.Task;
  }

  public System.Threading.Tasks.Task<csp.multiplayer.NumberOfRepliesResult> GetNumberOfRepliesAsync(System.Action<float>? progressCallback = null)
  {  
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.NumberOfRepliesResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.NumberOfRepliesResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:D:\a\connected-spaces-platform-unity\connected-spaces-platform-unity\interface\swigutils\AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.NumberOfRepliesResultCallback callback =
        new ConnectedSpacesPlatformDotNet.NumberOfRepliesResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.multiplayer.NumberOfRepliesResult>? handler = null;
    handler = new ((csp.multiplayer.NumberOfRepliesResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(GetNumberOfRepliesAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    GetNumberOfReplies(callback);

    return tcs.Task;

  }

  public System.Threading.Tasks.Task<csp.multiplayer.AnnotationResult> GetConversationAnnotationAsync(System.Action<float>? progressCallback = null)
  {  
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.AnnotationResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.AnnotationResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:D:\a\connected-spaces-platform-unity\connected-spaces-platform-unity\interface\swigutils\AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.AnnotationResultCallback callback =
        new ConnectedSpacesPlatformDotNet.AnnotationResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.multiplayer.AnnotationResult>? handler = null;
    handler = new ((csp.multiplayer.AnnotationResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(GetConversationAnnotationAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    GetConversationAnnotation(callback);

    return tcs.Task;

  }


  public System.Threading.Tasks.Task<csp.multiplayer.AnnotationResult> SetConversationAnnotationAsync(csp.multiplayer.AnnotationUpdateParams annotationParams,csp.systems.BufferAssetDataSource annotation,csp.systems.BufferAssetDataSource annotationThumbnail, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.AnnotationResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.AnnotationResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:D:\a\connected-spaces-platform-unity\connected-spaces-platform-unity\interface\swigutils\AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.AnnotationResultCallback callback =
        new ConnectedSpacesPlatformDotNet.AnnotationResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.multiplayer.AnnotationResult>? handler = null;
    handler = new ((csp.multiplayer.AnnotationResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(SetConversationAnnotationAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    SetConversationAnnotation(annotationParams,annotation,annotationThumbnail, callback);

    return tcs.Task;
  }

  public System.Threading.Tasks.Task<csp.systems.NullResult> DeleteConversationAnnotationAsync(System.Action<float>? progressCallback = null)
  {  
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:D:\a\connected-spaces-platform-unity\connected-spaces-platform-unity\interface\swigutils\AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.NullResultCallback callback =
        new ConnectedSpacesPlatformDotNet.NullResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.NullResult>? handler = null;
    handler = new ((csp.systems.NullResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(DeleteConversationAnnotationAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    DeleteConversationAnnotation(callback);

    return tcs.Task;

  }


  public System.Threading.Tasks.Task<csp.multiplayer.AnnotationResult> GetAnnotationAsync(string messageId, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.AnnotationResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.AnnotationResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:D:\a\connected-spaces-platform-unity\connected-spaces-platform-unity\interface\swigutils\AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.AnnotationResultCallback callback =
        new ConnectedSpacesPlatformDotNet.AnnotationResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.multiplayer.AnnotationResult>? handler = null;
    handler = new ((csp.multiplayer.AnnotationResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(GetAnnotationAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    GetAnnotation(messageId, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.multiplayer.AnnotationResult> SetAnnotationAsync(string messageId,csp.multiplayer.AnnotationUpdateParams updateParams,csp.systems.BufferAssetDataSource annotation,csp.systems.BufferAssetDataSource annotationThumbnail, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.AnnotationResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.AnnotationResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:D:\a\connected-spaces-platform-unity\connected-spaces-platform-unity\interface\swigutils\AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.AnnotationResultCallback callback =
        new ConnectedSpacesPlatformDotNet.AnnotationResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.multiplayer.AnnotationResult>? handler = null;
    handler = new ((csp.multiplayer.AnnotationResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(SetAnnotationAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    SetAnnotation(messageId,updateParams,annotation,annotationThumbnail, callback);

    return tcs.Task;
  }


  public System.Threading.Tasks.Task<csp.systems.NullResult> DeleteAnnotationAsync(string messageId, System.Action<float>? progressCallback = null)
  {
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.systems.NullResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:D:\a\connected-spaces-platform-unity\connected-spaces-platform-unity\interface\swigutils\AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.NullResultCallback callback =
        new ConnectedSpacesPlatformDotNet.NullResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.systems.NullResult>? handler = null;
    handler = new ((csp.systems.NullResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(DeleteAnnotationAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    // callback is defined in MAKE_AWAITABLE_CALLBACK_BODY
    DeleteAnnotation(messageId, callback);

    return tcs.Task;
  }

  public System.Threading.Tasks.Task<csp.multiplayer.AnnotationThumbnailCollectionResult> GetAnnotationThumbnailsForConversationAsync(System.Action<float>? progressCallback = null)
  {  
    // Create a TaskCompletionSource to represent the async operation.
    System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.AnnotationThumbnailCollectionResult> tcs = 
        new System.Threading.Tasks.TaskCompletionSource<csp.multiplayer.AnnotationThumbnailCollectionResult>(System.Threading.Tasks.TaskCreationOptions.RunContinuationsAsynchronously);

    /*@SWIG:D:\a\connected-spaces-platform-unity\connected-spaces-platform-unity\interface\swigutils\AsyncAdapters.i,129,MAKE_ROOTED_ASYNC_CALLBACK_BODY@*/
    ConnectedSpacesPlatformDotNet.AnnotationThumbnailCollectionResultCallback callback =
        new ConnectedSpacesPlatformDotNet.AnnotationThumbnailCollectionResultCallback();

    // Define the callback that will be called by the C++ code
    System.Action<csp.multiplayer.AnnotationThumbnailCollectionResult>? handler = null;
    handler = new ((csp.multiplayer.AnnotationThumbnailCollectionResult result) =>
    {
        try
        {
           /* This is a bit jank. Due to the desire to use passthrough macros between async
            * and actions, we have this result param which yes, can be
            * a comma separated list for action style callbacks, but in async (awaitable) functions,
            * it's only ever a single name.
            * It ISNT always a ResultBase type (although that would simplify things if CSP would do that...)
            * Sometime's it's just a space entity, or a bool, or something else. */

            if((object)result is csp.systems.ResultBase _result)
            {
                // It's a result base

                // Convert the failing result to a throw if we have that option enabled
                _result.ThrowOnFailure(nameof(GetAnnotationThumbnailsForConversationAsync));


                // Use _result here because we need to call ResultBase api, but we still pass the fully specified type in the TrySetResult
                // Remember that callbacks also have `init` and `inProgress` status's. We will often receive this callback more than once,
                // only finish when we get a final status.  
                if (_result.GetResultCode() == csp.systems.EResultCode.Success ||
                    _result.GetResultCode() == csp.systems.EResultCode.Failed)
                {
                    tcs.TrySetResult(result);
                    // Now that the callback has been invoked and returned success or failure, we no longer need to
                    // await, so we unsubscribe, which should empty the invocation list and unroot the reference for GC. 
                    // Instead, if the result was still pending we would have still needed to await and keep this alive.
                    callback.Invoked -= handler;
                }
                else if (_result.GetResultCode() == csp.systems.EResultCode.InProgress)
                {
                    // Trigger the injected progress callback.
                    progressCallback?.Invoke(_result.GetRequestProgress());
                }
            }
            else
            {
                // It's something else
                tcs.TrySetResult(result);
                // Now that the callback has been invoked, we unsubscribe, so that if there is no other subscriber
                // the callback gets automatically unrooted. We do this because we do not expect
                // any more callbacks to come since this is not a ResultBase with pending status. This will
                // trigger the GC of the callback safely, since we no longer await for it.
                callback.Invoked -= handler;
            }
        }
        catch (System.Exception ex)
        {
            // If any other exception occurs, we set it on the task completion source. Failsafe.
            tcs.TrySetException(ex);
        }
    });

    // Subscribe to the callback. 
    // Note that this will automatically root the callback since it has at least one subscriber.
    callback.Invoked += handler;

/*@SWIG@*/;

    // Run the method with the provided arguments and the callback
    GetAnnotationThumbnailsForConversation(callback);

    return tcs.Task;

  }


    // Single native action adapter instance for this event
    private ConnectedSpacesPlatformDotNet.ConversationNetworkEventCallback? _OnConversationUpdateAdapter;

    /// <summary>
    /// C# event exposing the native callback.
    /// Subscribers are automatically registered/unregistered with native code.
    /// </summary>
    public event System.Action<csp.common.ConversationNetworkEventData> OnConversationUpdate
    {
        add
        {
            if (_OnConversationUpdateAdapter == null)
            {
                // First subscriber, create adapter. Note that this automatically subscribes the passed value.
                _OnConversationUpdateAdapter = new ConnectedSpacesPlatformDotNet.ConversationNetworkEventCallback(value);
                // Register with native code
                SetConversationUpdateCallback(_OnConversationUpdateAdapter);
            }
            else
            {
                // We already had an adapter existing, just subscribe to the event.
                _OnConversationUpdateAdapter!.Invoked += value;
                // Note: we should not need to call the SetConversationUpdateCallback again since it was supposed to be set on adapter creation.
            }
        }
        remove
        {
            if (_OnConversationUpdateAdapter == null)
            {
                // This should not happen
                var eventNameAdapter = nameof(_OnConversationUpdateAdapter);
                var eventName = nameof(OnConversationUpdate);

                UnityEngine.Debug.LogError($"{eventNameAdapter} is null when trying to remove subscriber from {eventName}.");

                return;
            }

            _OnConversationUpdateAdapter.Invoked -= value;

            if (!_OnConversationUpdateAdapter.HasSubscribers)
            {
                // Unregister from native code
                SetConversationUpdateCallback(null);

                // No more subscribers, clean up adapter
                _OnConversationUpdateAdapter = null;
            }
        }
    }


  // Any time this object is returned from an outer C++ object via reference, this is set
  // to prevent premature garbage collection causing premature C++ memory deallocation.
  public object OuterObjectPin { private get; set; }

  internal static csp.multiplayer.ConversationSpaceComponent NativeFromBaseCast(csp.multiplayer.ComponentBase baseObj) {
    global::System.IntPtr cPtr = ConnectedSpacesPlatformDotNetPINVOKE.csp_multiplayer_ConversationSpaceComponent_NativeFromBaseCast(csp.multiplayer.ComponentBase.getCPtr(baseObj));
    csp.multiplayer.ConversationSpaceComponent ret = (cPtr == global::System.IntPtr.Zero) ? null : new csp.multiplayer.ConversationSpaceComponent(cPtr, false);
    if (ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Pending) throw ConnectedSpacesPlatformDotNetPINVOKE.SWIGPendingException.Retrieve();
    return ret;
  }

}

}
